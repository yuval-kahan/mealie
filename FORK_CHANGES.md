# שינויים שבוצעו ב-fork

עודכן לאחרונה: 2026-07-26

מסמך זה מתעד את ההתאמות שבוצעו ב-fork המקומי של Mealie, כדי שיהיה קל להבין מה נוסף לפני העלאה ל-GitHub.

## תקציר

ה-fork מוסיף ל-Mealie יכולות AI נוחות יותר, יצירת מתכון מטקסט חופשי/קישור/תמונה, חיפוש מתכונים עם AI בתוך המתכונים הקיימים, שדות מקור/יוצר למתכון, תמיכה רחבה בהעלאת וידאו למתכונים, תצוגת מדיה עליונה בתוך עמוד המתכון, ניהול API מהיר מהתפריט הצדדי, ניווט מהיר לספרים/קטגוריות/תגיות, מזווה אישי, שמירת אתרי קניות מזון, ומחיקה מהירה של מתכונים מרשימת המתכונים.

ה-README הראשי כולל כעת תקציר אנגלי של יכולות ה-fork וצילום מסך של תוסף Mealie AI, כדי שמבקרים ב-GitHub יבינו מיד מה נוסף לעומת Mealie המקורי.

## תרגום מלא של האתר ותוסף הדפדפן

- כל 42 השפות שכבר היו קיימות ב-Mealie נשמרו, בלי להוסיף שפה חדשה ובלי להסיר שפה קיימת.
- בסיס ה-fork עד 2026-07-13 הושלם בכל 42 שפות האתר עם 1,700 מפתחות תואמים וללא ערכים ריקים או placeholders שבורים.
- הפיצ'רים החדשים שנוספו לאחר השלמת בסיס 42 השפות מתורגמים כרגע לעברית ולאנגלית בלבד, לפי החלטת הפרויקט. בשאר השפות Vue i18n משתמש ב-fallback האנגלי עד שיוחלט להשלים אותן.
- בנוסף תורגמו יותר מ-4,000 ערכים ותיקים שנותרו באנגלית בתוך קובצי שפה אחרים. שמות מוצר ומונחים בינלאומיים כמו `Mealie`, `OpenAI`, `JSON`, `PDF` ו-`URL` נשמרו כאשר זה הניסוח הנכון בשפת היעד.
- תוקנו גם תרגומים ותיקים עם placeholders חסרים, כולל `{organize}`, וערך ריק שהיה בפורטוגזית-אירופית. הבדיקה מתחשבת במספר שונה של צורות רבים בין שפות ושומרת את קבוצת ה-placeholders הנדרשת.
- תוסף Chrome/Edge קיבל מערכת i18n מלאה עם 80 מפתחות בכל אחת מאותן 42 שפות:
  - שפת ממשק אוטומטית לפי שפת הדפדפן.
  - בחירה ידנית של שפת ממשק מתוך 42 השפות הקיימות בלבד.
  - RTL אוטומטי לערבית ולעברית.
  - תרגום של popup, הודעות סטטוס ושגיאה, קישורי תוצאה, preview, נתוני החילוץ, background worker ו-browser bridge.
  - fallback ל-`en-US` רק אם קובץ שפה לא נטען.
- metadata של ה-manifest נוצר עבור 37 קודי השפה הרשמיים שבהם Chrome Web Store תומך. אפריקאנס, גליסית, איסלנדית וגרסאות צרפתית אזוריות עדיין זמינות במלואן בתוך ה-popup דרך מערכת ה-runtime; Chrome משתמש בשפת manifest הקרובה ביותר לפי מגבלות הפלטפורמה.
- נוסף כלי תחזוקה `dev/scripts/translate_missing_locales.py` שמתרגם רק ערכים חסרים/ריקים/שבורים, שומר על placeholders, בודק generation אמיתי לפני שימוש במפתח AI ושומר כל שפה בצורה אטומית.
- נוסף validator בשם `dev/scripts/check_localization_parity.py`, והוא מחובר ל-`task locales:check` ול-`ui:check`. הבדיקה נכשלת אם רשימות השפה של האתר והתוסף שונות או אם חסר מפתח/ערך/placeholder.
- נוסף generator בשם `dev/scripts/generate_extension_manifest_locales.py` ליצירת `_locales` החוקיים של Chrome מתוך תרגומי ה-runtime, כדי למנוע כפילות ידנית.

## ניהול ספקי AI ו-API

- נוסף חלון API מהיר בתפריט הצדדי, מתחת להגדרות, כדי לפתוח ולנהל ספקי AI בלי להיכנס למסכי הגדרות עמוקים.
- נוספה רשימת ספקים מוכנה לבחירה:
  - OpenAI
  - Google Gemini
  - Anthropic Claude
  - Custom / Local OpenAI-compatible
- בחירת ספק מעדכנת אוטומטית:
  - שם ספק ברירת מחדל
  - Base URL
  - רשימת מודלים
  - מודל ברירת מחדל
- נוספה רשימת מודלים מובנית לכל ספק גדול.
- במודלים של Google Gemini נוספו הערות ליד המודל לגבי חינם / בתשלום ועלויות משוערות, כדי שיהיה קל לבחור מודל מתאים.
- נוספה תמיכה בכמה API keys עבור Google Gemini.
  אפשר להפריד מפתחות בשורה חדשה או בפסיק.
- עבור Gemini, המפתח הראשון משמש לקריאות AI רגילות. בחילוץ מתכונים מספר ובתרגום ספר, כל מפתח ייחודי הופך ל-worker נפרד והעבודה מתחלקת בין כולם.
- אם הוזנו מפתחות Gemini כפולים, הם מזוהים במסך ונשמרים פעם אחת בלבד, תוך שמירה על הסדר המקורי.
- גם צד השרת מנקה כפילויות במפתחות Gemini, כך שקריאות API ישירות לא יכולות לשמור מפתחות כפולים.
- נוספה בדיקת API key לפני שמירה.
  הבדיקה נעשית דרך endpoints של רשימת מודלים / גישה לספק, כדי להימנע מקריאת generation רגילה שעלולה לעלות כסף.
- אם המפתח לא תקין, אי אפשר לשמור את הספק.
- במסך עריכת ספק קיים מוצג האם הספק הוא ברירת המחדל או לא.
- ברשימת הספקים מוצגים גם שם המודל ו-Base URL, כדי שיהיה ברור באיזה מודל כל ספק משתמש.
- אם קיים ספק יחיד ולא נבחרה ברירת מחדל, הוא נבחר אוטומטית. אם קיימים כמה ספקים, ניתנת עדיפות אוטומטית ל-Google Gemini ורק אחר כך לספקים האחרים; בחירה מפורשת של המשתמש לעולם אינה נדרסת.
- ניתן לראות, לערוך, למחוק ולבחור ספקים קיימים מתוך חלון ה-API המהיר.

קבצים מרכזיים:

- `frontend/app/components/Domain/Group/GroupAIProviderDialog.vue`
- `frontend/app/components/Domain/Group/GroupAIProviderSettingsEditor.vue`
- `frontend/app/components/Layout/LayoutParts/AppSidebar.vue`
- `frontend/app/composables/use-ai-providers.ts`
- `mealie/services/openai/openai.py`
- `mealie/routes/groups/controller_group_ai_providers.py`
- `mealie/routes/admin/admin_management_ai_providers.py`
- `mealie/repos/repository_ai_provider.py`
- `mealie/schema/group/ai_providers.py`

## יצירת מתכון מטקסט חופשי בעזרת AI

- נוספה אפשרות ליצור מתכון מטקסט חופשי, למשל כתבה, טקסט שהועתק מאתר, או מתכון בעברית/אנגלית.
- נוסף כפתור מהיר בתפריט הצדדי: "יצירה עם AI".
- נוסף כפתור מהיר נוסף בתפריט הצדדי: "יצירת מתכון ידנית". הכפתור יוצר טיוטה זמנית ופותח מיד את מסך עריכת המתכון בלי לבקש שם מראש; שמירה נחסמת עד שמוזן שם מתכון אמיתי.
- הכפתור פותח popup מהיר שבו ניתן לבחור בין יצירה מטקסט, יצירה מקישור, או יצירה מתמונה.
- שלושת המצבים בלעדיים: או שמדביקים טקסט, או שמדביקים קישור למתכון באינטרנט, או שמעלים תמונה/תמונות של מתכון.
- ביצירה מקישור, החלון שולח את הקישור למסלול הייבוא עם `useOpenAI`, כך שה-AI מנסה לחלץ את המתכון מהדף.
- ביצירה מתמונה, החלון שולח את התמונות ל-endpoint הקיים של יצירת מתכון מתמונה, ויכול לצרף גם וידאו למתכון שנוצר.
- ביצירה מתמונה נוסף שדה הערות אופציונלי. אפשר לרשום תיקונים כמו "במקום שתי שמנות מתוקות, אחת מתוקה ואחת חמוצה", והפרומפט מגדיר הערות מפורשות של המשתמש כסמכותיות מול מה שה-AI מזהה בתמונה.
- בפופאפ "יצירה עם AI" נוספה אפשרות מסומנת כברירת מחדל ליצור גם רשימת קניות למתכון שנוצר.
- רשימת הקניות האוטומטית נוצרת אחרי שמירת המתכון, מקבלת את שם המתכון, מתמלאת מרכיבי המתכון, ואז נשלחת ל"סדר עם AI" כדי לקבל קטגוריות/תוויות מסודרות.
- אם "הערות וטיפים של AI" פעיל, גם סידור רשימת הקניות יכול להוסיף הערות קנייה קצרות לפריטים, למשל סוג עגבניות/פלפל/תפוח אדמה או איכות חומר גלם מומלצת.
- אם כבר קיימת רשימת קניות באותו שם, המערכת יוצרת שם פנוי עם מספר בסוגריים כדי לא לדרוס רשימה קיימת בלי אישור.
- בפופאפ "יצירה עם AI" נוספה גם העלאת תמונת מתכון נפרדת, לצד העלאת הווידאו. המערכת מנסה קודם למצוא ולהוריד תמונת מתכון מתאימה בעזרת מסלול התמונות האוטומטי; הקובץ שהמשתמש העלה משמש כתמונת כיסוי רק אם לא נשמרה תמונה אוטומטית.
- ביצירה מתמונה, מודל הראייה מסמן האם הקובץ שהועלה הוא צילום אמיתי של המנה המוכנה. צילום עמוד, מסמך, טקסט, מרכיבים, אריזה או שלב הכנה אינו משמש עוד כתמונת הכיסוי; החיפוש האוטומטי מבקש במפורש צילום של מנה מוכנה ומצולחת. תמונת המקור נשמרת כ-fallback רק כאשר היא עצמה תמונת מנה מתאימה.
- בפופאפ "יצירה עם AI" נוספה העלאת תמונות נוספות בשורה נפרדת. תמונות אלה נשמרות כ-assets של המתכון ומופיעות בגלריית המדיה של עמוד המתכון.
- נוסף endpoint ייעודי לתוסף דפדפן: התוסף מחלץ מהעמוד הפתוח JSON-LD של מתכון וטקסט גלוי, שולח את התוכן ל-AI, והשרת יוצר מתכון בלי שהשרת יצטרך לגשת בעצמו לאתר המקורי.
- endpoint התוסף יכול גם ליצור רשימת קניות אוטומטית למתכון ולסדר אותה עם AI. אם סידור רשימת הקניות נכשל, המתכון עדיין נשמר והתוסף מקבל הודעת אזהרה.
- נוסף תוסף Chrome/Edge מקומי תחת `browser-extension/mealie-recipe-importer`. התוסף משתמש בהתחברות הקיימת ל-Mealie בדפדפן, מאפשר להגדיר כתובת Mealie, לבחור שפת תרגום מתוך כל השפות הקיימות באתר, והאם ליצור/לסדר רשימת קניות. אין בתוסף מפתח AI נפרד.
- נוסף לתוסף אייקון מקורי של האות `M` בצבעי Mealie, כולל גדלים מתאימים ל-Chrome/Edge ולכפתור התוסף בדפדפן.
- בתוסף נוסף מצב "חילוץ מאמר" לצד "חילוץ מתכון". במצב מאמר, התוסף שולח את תוכן העמוד ל-Mealie, ה-AI יוצר מאמר מסודר, מתרגם לשפת היעד אם צריך, ומוסיף קטגוריות ותגיות.
- בתוסף נוסף מצב "זיהוי אוטומטי", והוא ברירת המחדל. במצב הזה ה-AI מחליט אם העמוד הוא מתכון, מאמר, או מאמר שמכיל מתכון, ושומר את התוכן בהתאם.
- חילוץ מאמר יודע לזהות שלושה מצבים: מאמר רגיל, מתכון בלבד, או מאמר שיש בתוכו מתכון. אם יש מתכון מלא בתוך המאמר, המאמר נשמר עם כל התוכן כולל המתכון, ובמקביל נוצר גם מתכון נפרד. אם העמוד הוא רק מתכון, המערכת יכולה ליצור רק מתכון ולא לשמור מאמר מזויף.
- אם במצב אוטומטי/מאמר נמצא מתכון מלא, התוסף יכול ליצור גם רשימת קניות אוטומטית ולסדר אותה עם AI בדיוק כמו חילוץ מתכון רגיל.
- שפת התרגום שנבחרה בתוסף נשלחת תמיד כחוזה פלט מחייב, גם כאשר זיהוי שפת העמוד חושב שהמקור כבר באותה שפה. כך המודל לא חוזר לאנגלית כברירת מחדל, וכל שדות המתכון נוצרים בשפת היעד שנבחרה.
- כאשר התוסף יוצר גם רשימת קניות, אותה שפת יעד מועברת גם לשלב הסידור עם AI. שמות הפריטים מגיעים מהמתכון המתורגם, וכותרות הקטגוריות והערות הקנייה שנוצרות ב-AI נדרשות לחזור באותה שפה.
- התוסף בודק מול Mealie האם מוגדר ספק AI פעיל. אם אין API/AI מוגדר באתר, כפתור השליחה ל-AI בתוסף חסום ומוצגת הודעה ברורה שצריך להגדיר API באתר.
- בדיקת ספק ה-AI בתוסף עודכנה לקרוא ישירות את הגדרות ספקי ה-AI של הקבוצה, כדי למנוע מצב שבו התוסף חושב שאין ספק למרות שהאתר כבר מוגדר.
- התוסף לא שומר מפתח API משלו. כדי לשלוח ל-Mealie מתוך popup של Chrome/Edge, הוא קורא את cookie ההתחברות של Mealie דרך הדפדפן, ואם צריך נופל חזרה לאיתור טאב פתוח של Mealie וקריאת token ההתחברות ממנו. ה-token משמש כ-Bearer token לבקשה הנוכחית בלבד.
- פעולות "חלץ ושלח ל-AI" ו"שמור אתר" הועברו ל-background service worker של התוסף. לאחר הלחיצה אפשר לסגור את ה-popup והחילוץ/שמירה ממשיכים ברקע; תג קטן על אייקון התוסף מציג כמה פעולות עדיין רצות. בזמן פעולה קיים timer יחיד לשמירת ה-worker פעיל, והוא מנוקה מיד כשהפעולה האחרונה מסתיימת. טקסט החילוץ מוגבל ל-180,000 תווים וה-preview שמוחזר ל-popup מוגבל ל-30,000 תווים כדי למנוע הצטברות זיכרון.
- אם התוסף לא מצליח למצוא התחברות פעילה ל-Mealie, מופיע כפתור "פתח/חבר את Mealie" שפותח או ממקד את הטאב של Mealie כדי שהמשתמש יוכל להתחבר ואז לחזור לתוסף.
- נוסף bridge בין האתר לתוסף: כאשר יצירה מקישור נכשלת בצד השרת בגלל חסימת אתר/גישה, עמוד "יצירה עם AI" מבקש מהתוסף לטפל בקישור אוטומטית. התוסף ממחזר לשונית קיימת של עמוד המקור אם היא כבר פתוחה; אחרת הוא פותח לשונית רקע זמנית, ממתין לטעינה, מחלץ את התוכן, שולח אותו ל-Mealie וסוגר רק את הלשונית שהוא עצמו יצר. אם התוסף לא מותקן, מוצגת הודעת הכשל הרגילה בלי להמתין זמן רב.
- בכל יצירת מתכון עם AI המערכת מנסה לשמור תמונת מתכון אוטומטית: קודם מתוך תמונת המתכון בעמוד המקור (`JSON-LD`, `og:image`, `twitter:image` או תמונה גדולה בדף), ואם אין תמונה בעמוד היא מנסה למצוא תמונה ציבורית מתאימה לפי שם המתכון. ביצירה מתמונה, התמונה שהועלתה לצורך קריאת המתכון היא fallback אחרון בלבד אם לא נמצאה תמונת מתכון מתאימה. כשל בתמונה לא חוסם את יצירת המתכון.
- תמונות מתכון אוטומטיות תמיד מועדפות כהורדה ושמירה מקומית. אם כל ניסיונות ההורדה נכשלו, המערכת שומרת URL חיצוני כ-fallback ומציגה אותו בכרטיס, בעמוד המתכון ובהדפסה. אם גם הקישור החיצוני שבור בדפדפן, כפתור "הוסף תמונה עם AI" חוזר להופיע כדי שאפשר יהיה לנסות שוב.
- כפתור "הוסף תמונה עם AI" בכרטיס מתכון קיים הוקשח: הוא לא מייצר תמונה חדשה, אלא קודם מנסה לחלץ תמונה מעמוד המקור של המתכון, אחר כך מחפש תמונה ציבורית עם שאילתות קצרות ומדויקות, ואם אין תוצאות בעברית הוא משתמש בספק ה-AI כדי ליצור מילות חיפוש באנגלית ואז מנסה שוב. החיפוש מוגבל במספר מועמדים, ב-timeout קצר, בקריאת HTML חלקית ובעצירת סריקה מוקדמת כדי למנוע עומס או הצטברות קריאות.
- בתפריט שלוש הנקודות בכרטיס מתכון נוסף חלון משותף להחלפת תמונת המתכון מקובץ או מקישור. מצב הקישור משתמש כברירת מחדל בתוסף הדפדפן ואינו מפעיל AI: התוסף פותח את כתובת התמונה בטאב רקע זמני, מוריד את קובץ התמונה באמצעות חיבור הדפדפן, מעלה את הקובץ ל-Mealie ושומר אותו מקומית עם המתכון. אפשר לבטל את הסימון ולנסות הורדה ישירה מהשרת.
- הורדת תמונה דרך התוסף מוגבלת ל-20MB ולפורמטים שבהם Mealie תומך. הטאב הזמני נסגר תמיד, וה-listener ושני ה-timeouts של גשר האתר מתנקים בהצלחה, בכשל וביציאה מהמסך, כדי שלא להצטבר בזיכרון.
- שם המקור וה-URL המלא נשמרים בנפרד ב-`extras` עבור יצירה מקישור ותוסף הדפדפן. בעמוד המתכון, בהעתקה ובהדפסה מוצגים גם שם המקור הקריא וגם כתובת הקישור המלאה, והקישור נפתח בטאב חדש.
- תמונת כיסוי ידנית אינה דורסת עוד תמונה שהמסלול האוטומטי כבר שמר. לפני העלאת ה-fallback ה-frontend קורא את המתכון פעם אחת ומעלה את הקובץ רק כאשר שדה התמונה עדיין ריק.
- שמות מתכונים שנוצרים עם AI מקבלים ייחוס קצר כאשר הוא מופיע במקור: קודם שף/יוצר/מסעדה, ואחר כך אתר או מקור. הייחוס אינו מומצא, אינו מוכפל אם הוא כבר בשם, ושמות זהים ממשיכים לקבל מספר עולה במקום לדרוס מתכון קיים. הכלל משותף לטקסט, תמונה, קישור, תוסף וחילוץ מספר.
- כל מסלולי ה-AI משתמשים כעת באותו סיווג משופר לקטגוריות ולתגיות: שם המנה המרכזי נוסף תמיד כתגית, נשמרת קטגוריית מנה רחבה, ונוספות תגיות `מישלן`, `גורמה` ו`ארוחה` רק כאשר התוכן מצדיק אותן. מישלן לעולם אינו מוסק רק מסגנון יוקרתי או משף מפורסם.
- קטגוריות שמתכון AI יוצר נשמרות בעברית בלבד. תגיות כלליות נשמרות בעברית ככל האפשר; רק שמות פרטיים של שפים ומסעדות רשאים להישמר גם בכתיב המקורי. שכבת השרת ממירה שמות אנגליים נפוצים לעברית ומונעת מקטגוריה מעורבת בעברית/אנגלית להישמר כפי שהיא.
- מתכון שנוצר או נערך עם AI מקבל גם התאמה לארוחות בוקר, צהריים וערב. ניתן להחזיר כמה תקופות מתאימות לאותו מתכון; ההתאמה נשמרת במתכון, מוצגת בעמוד המתכון ומתווספת כתגיות עבריות לחיפוש ולסינון.
- כאשר המקור נתמך, ה-AI מזהה גם טלוויזיה, Instagram, YouTube, TikTok, אתר, ספר בישול או מגזין, ומוסיף גם את שם התוכנית, החשבון, הערוץ, האתר או הספר בלי להמציא אותו. אותם כללים חלים על טקסט, תמונה, עריכה עם AI, סרטון וחילוץ מספר.
- בחלון `שנה תמונה עם AI` שדה ההנחיה נפתח כברירת מחדל עם שם המתכון הקיים. המשתמש יכול להשאיר אותו כפי שהוא או לערוך אותו לפני החיפוש.
- תוקנה תקלה שבה יצירה מטקסט עם AI נכשלה כאשר ה-AI החזיר קטגוריות/תגיות. מתכוני AI כבר לא עוברים דרך ניקוי יבוא האתרים שמחק מזהי קבוצה, וה-factory משלים `group_id` לקטגוריות/תגיות/כלים לפני שמירה.
- יצירה עם AI כבר לא מציגה את אפשרות "חילוץ רכיבים אחרי הייבוא" ולא פותחת אוטומטית את חלון "חילוץ רכיבים" הישן אחרי שמירת המתכון. זה חל גם על יצירה מטקסט/קישור/תמונה וגם על מסך יצירה מתמונה הישן. ה-AI כבר מחזיר רכיבים מסודרים, ולכן החלון הזה גרם לבלבול והודעות שגיאה מיותרות. עדיין ניתן להפעיל פענוח רכיבים ידנית מתוך עריכת המתכון אם צריך.
- עמוד המתכון לא יפתח אוטומטית את חלון פענוח הרכיבים דרך `?parse=true` אם כבר קיימים רכיבים עם יחידה או מזון מפוענחים.
- נוסף גם route ייעודי ליצירה מטקסט: `/r/create/text`.
- ה-AI מחזיר מבנה מסודר לפי schema, ולא טקסט חופשי.
- לפני יצירת מתכון, ה-AI מחזיר גם `is_recipe`.
  אם הטקסט לא נראה כמו מתכון, או חסרים שם/רכיבים/הוראות, לא נוצר מתכון והמשתמש מקבל הודעה ברורה.
- נוסף חיזוק לפרומפט כדי לא ליצור מתכון מדף קטגוריה/חיפוש/אינדקס שמציג כמה מתכונים. במקרה כזה ה-AI אמור להחזיר שזה לא דף מתכון יחיד, במקום לערבב כמה כרטיסי מתכונים.
- נוסף checkbox מסומן כברירת מחדל: "הוסף הערות, טיפים והמלצות סוגי רכיבים עם AI". כשהוא פעיל, ה-AI מוסיף הערות וטיפים שימושיים למתכון, וממלא בצורה יזומה יותר את "סוג מומלץ" לרכיבים שבהם סוג חומר הגלם משנה, כמו עגבניות, פלפלים, תפוחי אדמה, עשבי תיבול, גבינות, נתחי בשר, אורז וצורות פסטה. כשהוא כבוי, ה-AI מתמקד בחילוץ וסידור המתכון בלי להוסיף טיפים משלו.
- השדות הנתמכים כוללים:
  - שם מתכון
  - תיאור
  - כמות / מנות
  - זמני הכנה / בישול / זמן כולל
  - מרכיבים עם כותרות קבוצות
  - סוג מומלץ לרכיב, למשל סוג עגבנייה, פלפל, תפוח אדמה, עשב תיבול, גבינה, נתח בשר או צורת פסטה
  - הוראות הכנה
  - הערות
  - קטגוריות
  - תגיות
  - כלים / ציוד
- נוספה אפשרות לתרגם את המתכון לשפת הממשק בזמן היצירה.
- נוספה אפשרות להריץ parsing למרכיבים אחרי הייבוא.
- קטגוריות, תגיות וכלים שה-AI מחזיר נוצרים או ממוחזרים אוטומטית לפי מה שכבר קיים בקבוצה.
- נוסף שדה אופציונלי ברמת רכיב בשם "סוג מומלץ". השדה זמין בעריכה ידנית, מוצג בעמוד המתכון ובהדפסה, ומשתתף בחיפוש הרגיל ובאינדקס חיפוש AI.
- נוסף prompt ייעודי שמנחה את ה-AI לא לדלג על תוכן שימושי, לא להמציא מידע, ולשמור ציוד/הגשה/מקור בהערות או בכלים לפי הצורך.

קבצים מרכזיים:

- `frontend/app/components/Domain/Recipe/RecipeCreateFromTextForm.vue`
- `frontend/app/pages/g/[groupSlug]/r/create/text.vue`
- `frontend/app/pages/g/[groupSlug]/r/create.vue`
- `frontend/app/components/Layout/DefaultLayout.vue`
- `frontend/app/lib/api/user/recipes/recipe.ts`
- `frontend/app/plugins/axios.ts`
- `mealie/core/exceptions.py`
- `mealie/routes/recipe/recipe_crud_routes.py`
- `mealie/schema/recipe/recipe_scraper.py`
- `mealie/services/recipe/recipe_service.py`
- `mealie/services/scraper/scraper.py`
- `mealie/schema/openai/recipe.py`
- `mealie/services/openai/prompts/recipes/parse-recipe-text.txt`
- `browser-extension/mealie-recipe-importer`

## יצירה ידנית גמישה והתאמת כמויות

- מסך היצירה הידנית סודר מחדש לקבוצות ברורות של פרטי בסיס, מנות/תשואה, זמנים, תיאור, יוצר ומקור, בלי להסיר שדות קיימים.
- ביצירה ידנית ניתן לבחור מצב אחד בלבד:
  - מתכון מובנה עם רכיבים, כלים והוראות כמו ב-Mealie הרגיל.
  - טקסט פשוט ששומר מעברי שורה ומוצג כפי שהודבק.
  - טקסט מעוצב ב-Markdown שמוצג דרך רכיב ה-Markdown הבטוח של Mealie.
- במצב טקסט פשוט/מעוצב אין ניסיון אוטומטי לפרק את התוכן לשדות, והרכיבים/ההוראות המובנים מוסתרים כדי לא להציג שתי גרסאות של אותו מתכון.
- ליד רכיב בעל כמות מספרית נוסף כפתור שינוי כמות. הזנת כמות חדשה לרכיב אחד מחשבת יחס חדש לכל המתכון: לדוגמה 500 גרם קמח ו-5 ביצים הופכים ל-250 גרם ו-2.5 ביצים, או ל-200 גרם קמח אם בוחרים 2 ביצים.
- הסקייל הוא שינוי תצוגה לאותה הפעלה ואינו דורס את כמויות הבסיס השמורות במתכון.
- בוחרי קטגוריות, תגיות וכלים כוללים הצעות מוכנות בעברית ובאנגלית. ההצעות אינן מעמיסות את מסד הנתונים מראש; ערך נוצר רק כשהמשתמש בוחר בו, ותמיד ניתן ליצור ערך חדש שאינו ברשימה.

## חיפוש מתכונים עם AI

- יכולות "מצא מתכון" הוטמעו בתוך עמוד "מתכונים", כדי שלא יהיה צורך בדף נפרד או בפריט ניווט נפרד.
- בסרגל הצד הוסר הקישור הנפרד "מצא מתכון"; הכניסה מתבצעת מתוך עמוד "מתכונים" דרך כפתור "מצא מתכון".
- כפתור "מצא מתכון" הועבר לשורת כפתורי החיפוש/מיון של עמוד המתכונים, בצמוד לכפתור המיון "נוצר", כדי שלא יעמוד לבד באמצע המסך.
- פאנל "מצא מתכון" בתוך עמוד המתכונים סודר מחדש עם רוחב מוגבל, אזור בחירת רכיבים/כלים ברור, ואזור נפרד לחיפוש AI ותוצאות.
- קישורים ישנים אל `/recipes/finder` עדיין לא נשברים: הם מפנים לעמוד "מתכונים" ופותחים את פאנל "מצא מתכון" אוטומטית.
- בפאנל "מצא מתכון" בתוך עמוד המתכונים נשמרו היכולות הקיימות: בחירת רכיבים, בחירת כלים, הגדרות חוסר מותר, פילטרים נוספים, הצעות "מוכן להכנה", הצעות "כמעט מוכן", וחיפוש AI.
- בפאנל "מצא מתכון" נוספה אפשרות לחפש מתכונים בשפה חופשית בעזרת AI.
- המשתמש יכול לכתוב בקשה כמו "משהו איטלקי עם גבינות" או "ארוחת ערב בלי בשר", וה-AI מחפש בתוך המתכונים הקיימים.
- החיפוש בונה אינדקס מקומי מקוצר של המתכונים בקבוצה:
  - שם מתכון
  - תיאור
  - מקור ויוצר / שף / ספר
  - קטגוריות
  - תגיות
  - כלים
  - רכיבים עיקריים
  - הוראות והערות מקוצרות
- ה-AI מקבל רק shortlist של מועמדים מתוך האינדקס, מחזיר רק `slug` של מתכונים קיימים, והשרת מסנן כל תוצאה שלא באמת קיימת במסד.
- התוצאות מוצגות ככרטיסי מתכון רגילים, עם הסבר קצר למה ה-AI בחר כל מתכון.
- ניתן לבחור כמה תוצאות להחזיר.
- כפתור ניקוי מחזיר את המסך להצעות הרגילות לפי מרכיבים.
- נוסף אינדקס חיפוש AI מקומי ושמור במסד הנתונים.
- לכל מתכון נשמרים:
  - תקציר JSON קצר לשליחה ל-AI.
  - טקסט חיפוש מאוחד.
  - וקטור חיפוש מקומי.
  - hash ותאריך עדכון כדי לזהות מתי צריך לרענן את הקאש.
- בזמן חיפוש, השרת מעדכן רק מתכונים חדשים/משתנים באינדקס.
- רשומת אינדקס מתרעננת אוטומטית גם אחרי 24 שעות, כדי למנוע קאש ישן אם שינוי פנימי לא עדכן את timestamp של המתכון.
- השרת מדרג מקומית את המתכונים לפי הבקשה, ובונה shortlist של מועמדים רלוונטיים.
- ל-AI נשלח רק ה-shortlist ולא כל המתכונים, כדי לחסוך זמן, טוקנים ועלויות.
- ה-AI עדיין מסנן ומחזיר רק מתכונים קיימים מתוך ה-shortlist.

קבצים מרכזיים:

- `frontend/app/pages/g/[groupSlug]/recipes/finder/index.vue`
- `frontend/app/components/Domain/Recipe/RecipeFinderPanel.vue`
- `frontend/app/components/Domain/Recipe/RecipeExplorerPage/RecipeExplorerPage.vue`
- `frontend/app/components/Layout/DefaultLayout.vue`
- `frontend/app/lib/api/user/recipes/recipe.ts`
- `mealie/db/models/recipe/ai_search_index.py`
- `mealie/alembic/versions/2026-07-07-20.25.00_b5c9d1a8f0e6_add_recipe_ai_search_index.py`
- `mealie/routes/recipe/recipe_crud_routes.py`
- `mealie/services/recipe/recipe_service.py`
- `mealie/schema/recipe/recipe_ai_search.py`
- `mealie/schema/openai/recipe_search.py`
- `mealie/services/openai/prompts/recipes/search-recipes.txt`

## מאמרים

- נוסף אזור חדש בשם "מאמרים" בסרגל הצד.
- נוסף כפתור "יצירת מאמר" מתחת ל"מאמרים" בסרגל הצד. הכפתור פותח popup מהיר מעל המסך הנוכחי בלי לנווט לדף אחר.
- דף המאמרים בנוי בדומה למתכונים: כרטיסים, חיפוש טקסט, סינון לפי קטגוריות ותגיות, וכניסה לעמוד מאמר מלא.
- ניתן ליצור מאמר ידנית עם כותרת, מקור, כותב/יוצר, תקציר, תוכן מלא, קטגוריות ותגיות.
- ניתן ליצור מאמר עם AI מתוך טקסט מלא שהודבק למערכת.
- ניתן ליצור מאמר עם AI מתוך קישור: השרת מושך את תוכן הדף, מנקה HTML, ושולח ל-AI כדי לבנות מאמר מסודר.
- ביצירת מאמר עם AI שפת היעד נקבעת אוטומטית לפי שפת התצוגה של האתר. אם המקור בשפה אחרת, ה-AI מחזיר כותרת, תקציר, תוכן, קטגוריות ותגיות בשפת הממשק; אם המקור כבר באותה שפה, ה-AI רק מסדר ומתייג בלי לתרגם מחדש.
- ה-AI מחזיר מבנה JSON מסודר עם `is_article`, כותרת, תקציר, תוכן, מקור, כותב, קטגוריות ותגיות.
- אם הקלט לא נראה כמו מאמר או טקסט לימודי שימושי, השרת לא יוצר רשומה ריקה ומחזיר שגיאה ברורה.
- נוסף חיפוש AI בתוך מאמרים: המשתמש כותב בשפה חופשית מה הוא מחפש, וה-AI מחזיר רק מזהים של מאמרים קיימים עם הסבר קצר.
- בעמוד מאמר מלא נוסף כפתור "העתק" שמעתיק את המאמר בצורה מסודרת ללוח, כדי לשלוח בוואטסאפ או במקום אחר.
- נוסף migration למסד הנתונים עבור טבלת `articles`.

קבצים מרכזיים:

- `frontend/app/pages/articles/index.vue`
- `frontend/app/pages/articles/[id].vue`
- `frontend/app/components/Domain/Article/ArticleOrganizerInputs.vue`
- `frontend/app/lib/api/user/articles.ts`
- `frontend/app/lib/api/types/article.ts`
- `mealie/db/models/household/article.py`
- `mealie/routes/households/controller_articles.py`
- `mealie/schema/household/article.py`
- `mealie/schema/openai/article.py`
- `mealie/services/openai/prompts/articles/parse-article.txt`
- `mealie/services/openai/prompts/articles/search-articles.txt`
- `mealie/alembic/versions/2026-07-08-03.00.00_c4d5e6f7a8b9_add_articles.py`

## מקור ויוצר למתכון

- נוספו למתכון שני שדות חדשים:
  - `source` - מקור חופשי, למשל אתר, בלוג, כתבה, תוכנית טלוויזיה, ספר, עמוד בספר או מגזין.
  - `createdBy` - יוצר / שף / ספר / ערוץ / ארגון שמקושר למתכון.
- השדות מוצגים בראש עמוד המתכון אם יש להם ערך.
- אם `source` נראה כמו קישור אינטרנט, הוא מוצג כקישור לחיץ שנפתח בטאב חדש.
- בעמוד המתכון, תיאור המתכון, מקור/יוצר, תשואה ו"נעשה לאחרונה" מיושרים לאזור עמודת "מרכיבים" בדסקטופ, והמקור מופיע מתחת ליוצר.
- דירוג הכוכבים בראש עמוד המתכון מוצג במצב קריאה בלבד כברירת מחדל; כדי לשנות דירוג צריך ללחוץ קודם על כפתור עריכה ליד הכוכבים, כדי למנוע שינוי בטעות.
- בעברית, כותרת ההוראות בעמוד המתכון מוצגת כ"אופן ההכנה", ומספרי השלבים מוצגים כמספר בלבד בלי המילה "שלב".
- כפתורי הפעולה של עמוד המתכון, כולל שלוש נקודות, עריכה, פעולות מהירות ומועדפים, הועברו לקו האפור העליון שמתחת לשם ולדירוג המתכון.
- בעמוד המתכון נוספו כפתורי העתקה לאופן ההכנה בלבד, ולמרכיבים + אופן ההכנה יחד.
- סימון שלב הכנה ב-V כבר לא ממזער או מסתיר את תוכן השלב; השלב נשאר פתוח ונראה כמו לפני הסימון.
- נוספה העדפת משתמש להצגת קו חוצה על מרכיבים ושלבי הכנה שסומנו ב-V. ברירת המחדל פעילה, וניתן לכבות אותה במסך הגדרות המשתמש.
- פרטי "תשואה", "נעשה לאחרונה" וזמני הכנה/בישול/סה"כ זמן בעמוד המתכון מוצגים באותה שורת מידע רחבה במסכי דסקטופ, מיושרים לצד פרטי התיאור/יוצר/מקור בלי לדחוס את הפרטים לצד אחד.
- השדות ניתנים לעריכה במסך עריכת פרטי המתכון.
- השדות מופיעים גם בהדפסת מתכון.
- חיפוש המתכונים הרגיל יכול למצוא לפי מקור או יוצר.
- חיפוש ה-AI מקבל את השדות האלה בקטלוג, ולכן אפשר לבקש למשל "ריזוטו של שף מסוים" או "מתכון מספר מסוים".
- יצירת מתכון בעזרת AI יודעת למלא את השדות אם הטקסט כולל מקור, קרדיט, שם שף, ספר, תוכנית, בלוג או כתבה.
- ייבוא מאתר מנסה לשמור `author`/`publisher` קיימים בתור יוצר/מקור.
- נוספה migration למסד הנתונים עבור השדות והאינדקסים שלהם.

קבצים מרכזיים:

- `mealie/db/models/recipe/recipe.py`
- `mealie/alembic/versions/2026-07-07-20.10.00_f8a1c3d9e2b4_add_recipe_source_fields.py`
- `mealie/schema/recipe/recipe.py`
- `mealie/schema/openai/recipe.py`
- `mealie/services/recipe/recipe_service.py`
- `mealie/services/scraper/cleaner.py`
- `mealie/services/scraper/scraper_strategies.py`
- `frontend/app/components/Domain/Recipe/RecipePage/RecipePageParts/RecipePageInfoEditor.vue`
- `frontend/app/components/Domain/Recipe/RecipePage/RecipePageParts/RecipePageInfoCard.vue`
- `frontend/app/components/Domain/Recipe/RecipePrintView.vue`

## תמיכה בווידאו במתכונים

- נוספה אפשרות להעלות קבצי וידאו למתכון.
- סיומות וידאו נתמכות:
  - `mp4`
  - `webm`
  - `mov`
  - `m4v`
  - `ogv`
- השרת מגיש וידאו כ-inline media עם MIME type מתאים, כך שהדפדפן יכול להציג נגן במקום להוריד את הקובץ.
- נוספה קומפוננטת העלאת וידאו משותפת לשימוש בכל מסכי היצירה.
- נוסף helper שמאפשר לצרף וידאו למתכון אחרי יצירה.
- וידאו נתמך במסכי היצירה הבאים:
  - יצירה ידנית
  - יצירה מטקסט
  - יבוא מ-URL
  - יבוא URL מרובה
  - יבוא HTML / JSON
  - יצירה מתמונה
  - יבוא ZIP
- בייבוא URL מרובה נוספה תמיכה בווידאו לכל שורה, כולל endpoint multipart ייעודי ושיוך הווידאו למתכון הנכון לאחר סיום הייבוא.

קבצים מרכזיים:

- `frontend/app/components/Domain/Recipe/RecipeVideoAssetUpload.vue`
- `frontend/app/composables/use-recipe-video-asset.ts`
- `frontend/app/pages/g/[groupSlug]/r/create/new.vue`
- `frontend/app/pages/g/[groupSlug]/r/create/text.vue`
- `frontend/app/pages/g/[groupSlug]/r/create/url.vue`
- `frontend/app/pages/g/[groupSlug]/r/create/bulk.vue`
- `frontend/app/pages/g/[groupSlug]/r/create/html.vue`
- `frontend/app/pages/g/[groupSlug]/r/create/image.vue`
- `frontend/app/pages/g/[groupSlug]/r/create/zip.vue`
- `mealie/routes/media/media_recipe.py`
- `mealie/routes/recipe/recipe_crud_routes.py`
- `mealie/services/scraper/recipe_bulk_scraper.py`

## ספריית סרטונים ושמירה עם yt-dlp

- נוסף מסך `סרטונים` נפרד מתחת ל`אתרים`, לצד פעולת `שמירת סרטון מהירה` בניווט.
- אפשר לשמור סרטון באופן ידני או לתת ל-AI לנתח כתובת וידאו ולמלא כותרת, תיאור, פלטפורמה, ערוץ/יוצר, שפה, משך, תאריך, תמונה, קטגוריות ותגיות.
- ניתוח AI של סרטון מתבצע ישירות באמצעות Google Gemini על הווידאו עצמו, כולל התמונה והאודיו. המערכת אינה מפיקה תמלול מקומי ואינה מורידה כתוביות לצורך הניתוח.
- ב-YouTube כתובת הסרטון נשלחת ישירות ל-Gemini. בסרטון מאתר אחר, הקובץ מורד זמנית ומועלה ל-Gemini Files API; הקובץ הזמני ב-Gemini נמחק לאחר הניתוח.
- כאשר נשמרו כמה מפתחות Gemini, ניתוח שנכשל עקב rate limit או שגיאת שרת עובר למפתח הבא. אין צורך להגדיר מפתח AI נפרד בתוסף.
- ה-AI רשאי ליצור מתכון ורשימת קניות מקושרים רק כאשר בזרם החזותי או הקולי קיים מתכון מלא. הוא מונחה לא להמציא כמויות או שלבים חסרים.
- מתכון שחולץ מסרטון מקבל קטגוריות, תגיות, כלים, הערות, המלצות לזני רכיבים ותכנית `Mise en place` הכוללת גם את הכלים שיש להכין מראש.
- תמונה ממוזערת של הסרטון משמשת כתמונת המתכון רק אם Gemini קבע שהיא מציגה בבירור את המנה המוגמרת. אחרת המערכת מחפשת ומורידה תמונה של המנה המוגמרת.
- הורדת הסרטון עם `yt-dlp` פעילה כברירת מחדל, אך אפשר לכבות אותה. גם אם ההורדה נכשלת, הקישור והמטא-דאטה נשמרים בספרייה ואפשר לנסות שוב.
- הגדרות ההורדה משותפות לאתר ולתוסף וכוללות:
  - איכות מיטבית או מגבלת `2160p`, `1080p`, `720p` ו-`480p`
  - מכולה `MP4`, `MKV` או `WebM`
  - codec אוטומטי, `H.264`, `H.265`, `VP9` או `AV1`
  - מצב אודיו בלבד ואיכות אודיו
  - שמירת תמונה ממוזערת ומטא-דאטה
  - fallback אוטומטי לאיכות נמוכה יותר במקרה הצורך
- עבודת המטא-דאטה, ההורדה וניתוח ה-AI מתבצעת ברקע בשרת. סגירת popup התוסף או מעבר למסך אחר אינה מפסיקה את העבודה.
- כרטיסי הסרטונים מציגים סטטוס והתקדמות, נגן לקובץ שהורד, תמונה ממוזערת, קישורים למתכון ולרשימת הקניות, ופעולות ביטול, ניסיון חוזר, עריכה ומחיקה.
- בעת הפעלה מחדש של השרת, עבודות סרטונים שנקטעו חוזרות לתור וממשיכות אוטומטית.
- כתובות YouTube נפוצות עוברות canonicalization, ופרמטרי tracking מוסרים כדי למנוע שמירה כפולה של אותו סרטון.
- מחיקת סרטון אינה מוחקת כברירת מחדל את המתכון או רשימת הקניות המקושרים; המשתמש יכול לבחור אותם בנפרד בחלון המחיקה.
- תוסף הדפדפן קיבל פעולת `שמור סרטון` עצמאית. הוא משתמש בחיבור ובספק ה-AI שכבר מוגדרים ב-Mealie ואינו שומר מפתח API נפרד.

קבצים מרכזיים:

- `frontend/app/pages/videos/index.vue`
- `frontend/app/lib/api/user/videos.ts`
- `mealie/routes/households/controller_videos.py`
- `mealie/services/videos/video_library_service.py`
- `mealie/db/models/household/video.py`
- `browser-extension/mealie-recipe-importer/popup.js`
- `browser-extension/mealie-recipe-importer/background.js`

## עריכת מתכון, תמונה והכנה מראש עם AI

- בכל מסלולי יצירת המתכון עם AI אפשר לבקש תכנית `Mise en place` כברירת מחדל. התכנית מפרטת מה להכין מראש, אילו רכיבים שייכים לכל משימה ואילו כלים צריכים להיות מוכנים.
- התמיכה קיימת ביצירה מטקסט, תמונות, דף דפדפן, מאמר, חילוץ מספר וסרטון. אפשר לכבות אותה לפני היצירה.
- לאחר יצירת מתכון נוספה פעולת `עריכה עם AI` בתפריט של כרטיס המתכון ובעמוד המתכון. המשתמש כותב בקשה חופשית, מקבל טיוטה מלאה לעריכה ובדיקה, ורק לחיצה מפורשת על שמירה מעדכנת את המתכון.
- נוספה פעולת `שנה תמונה עם AI` גם למתכון שכבר יש לו תמונה. אפשר לכתוב הנחיה חופשית; התמונה הישנה נשארת עד שנמצאה והורדה תמונה חלופית תקינה.
- תכנית ההכנה מראש ניתנת לעריכה גם בחלון העריכה המהירה ומוצגת באזור פרטי המתכון.

## משאבים מקושרים

- נוספה אינדיקציה קטנה למשאבים מקושרים בכרטיסי מתכון, בתוך מתכון, בכרטיסי רשימות קניות, בתוך רשימת קניות ובכרטיסי סרטונים.
- לחיצה על האינדיקציה פותחת popup המחולק למתכונים, רשימות קניות, סרטונים ואתרי קניות, ומאפשר מעבר ישיר לכל משאב.
- ספירת הקישורים לכרטיסי מתכונים ורשימות קניות מחושבת בשאילתות bulk עבור העמוד הנוכחי ולא בבקשת API נפרדת לכל כרטיס.
- בחלון המחיקה המרוכזת של רשימות קניות מופיע הסימון ליד רשימה שמקושרת למשאב אחר, כדי למנוע מחיקה לא מודעת.

## תצוגת מדיה בראש עמוד המתכון

- תמונת המתכון הראשית מוצגת עכשיו בראש עמוד המתכון כ-Hero רחב, לפני שם המתכון ושאר פרטי המתכון, בדומה לאתרי מתכונים שבהם התמונה היא הפתיחה של העמוד.
- תמונת ה-Hero של המתכון נפרסת לרוחב מלא של אזור התוכן, בלי שוליים לבנים בצדדים, בעוד ששאר פרטי המתכון נשארים ברוחב קריא ומסודר.
- שאר תוכן המתכון נשאר מתחת לתמונה: שם, דירוג, מקור/יוצר, זמנים, רכיבים, הוראות, תגיות ופעולות.
- הווידאו מוצג כנגן אמיתי עם controls של הדפדפן:
  - Play / Pause
  - פס התקדמות
  - Volume
  - Fullscreen
- גלריית המדיה שמתחת לכותרת מציגה רק מדיה נוספת:
  - תמונות שהועלו כנכסים
  - וידאו שהועלה כנכס
- תמונת המתכון הראשית לא מופיעה יותר בגלריית המדיה, כדי למנוע כפילות.
- כרטיס "נכסים" בצד מסנן תמונות ווידאו, כדי למנוע כפילות. הוא נשאר לקבצים רגילים כמו PDF, טקסט וכו'.
- הסליידר ששלט בגודל המדיה העליונה הוסר; המדיה הנוספת מוצגת בפריסת grid אוטומטית ונקייה.

קבצים מרכזיים:

- `frontend/app/components/Domain/Recipe/RecipeMediaAssets.vue`
- `frontend/app/components/Domain/Recipe/RecipePage/RecipePageParts/RecipePageHeroImage.vue`
- `frontend/app/components/Domain/Recipe/RecipeAssets.vue`
- `frontend/app/components/Domain/Recipe/RecipePage/RecipePage.vue`
- `frontend/app/components/Domain/Recipe/RecipePage/RecipePageParts/RecipePageInfoCard.vue`
- `frontend/app/components/Domain/Recipe/RecipePage/RecipePageParts/RecipePageOrganizers.vue`

## שיפורי רשימת מתכונים ופעולות מהירות

- נוספה אפשרות למחוק מתכון מתוך תפריט שלוש הנקודות בכרטיס מתכון, בלי להיכנס לעמוד המתכון עצמו.
- פעולה זו נוספה גם לתצוגות כרטיסים שונות של מתכונים.
- שם הפעולה "הוסף לרשימה" עודכן ל"הוסף לרשימת קניות", כדי שיהיה ברור שהיא מוסיפה את רכיבי המתכון לרשימת קניות.
- בתפריט שלוש הנקודות, הוספה לרשימת קניות פותחת כברירת מחדל יצירת רשימת קניות חדשה בשם המתכון.
- מאותו חלון עדיין אפשר לבחור רשימת קניות קיימת, או ליצור רשימה חדשה בשם אחר. אם משאירים את שם הרשימה החדשה ריק, שם המתכון משמש כשם הרשימה.
- חלון ההוספה לרשימת קניות עובד עכשיו כזרימה אחת: בחירת רשימה/שם, סקירת רכיבים, הוספת פריטים ידניים נוספים ושמירה, בלי לפתוח חלון פנימי נפרד.
- אם מנסים ליצור רשימת קניות בשם שכבר קיים מתוך חלון המתכון, המשתמש חייב לבחור אם להוסיף לרשימה הקיימת או להחליף את התוכן הקיים שלה.
- במצב החלפה, הפריטים הקיימים ברשימת הקניות נמחקים ואז מתווספים הפריטים שנבחרו מהמתכון והפריטים הידניים.
- במסך רשימות הקניות הראשי נוספה חסימה ליצירת רשימת קניות חדשה בשם שכבר קיים, וגם צד השרת מחזיר שגיאת כפילות אם מנסים ליצור שם זהה דרך ה-API.
- בתפריט שלוש הנקודות נוסף "שנה שם מתכון", שמאפשר לערוך את שם המתכון במהירות בלי להיכנס למסך העריכה המלא.
- בתפריט שלוש הנקודות נוסף "העתק מתכון", שמעתיק טקסט מסודר של שם, מקור, יוצר, רכיבים, הוראות והערות ללוח, כדי לשלוח מהר בוואטסאפ או בכל מקום אחר.
- בכרטיסי המתכונים נוסף כפתור "העתק מתכון" גלוי על הכרטיס עצמו, גם בדסקטופ וגם במובייל.
- בכרטיסי המתכונים נוסף כפתור "העתק רשימת קניות" ליד העתקת המתכון. הפעולה פותחת או יוצרת רשימת קניות לפי שם המתכון, מסדרת אותה עם AI אם היא עדיין לא מסודרת, ורק אז מעתיקה אותה ללוח.
- בכרטיסי המתכונים ובדף המתכון נוסף כפתור "צור תמונות מצרכים עם AI". הכפתור מופיע רק אם עדיין לא נשמרו תמונות מצרכים למתכון, ובכרטיס הוא יושב על אזור התמונה בפינה העליונה בלי לשנות את גודל הכרטיס.
- בכרטיס מתכון נוסף כפתור מהיר שפותח popup של רשימת הקניות המקושרת. מתוך אותו חלון ניתן לשנות את שם הרשימה, לערוך או למחוק פריטים ולהוסיף פריט חדש; אם עדיין אין רשימה, החלון מציע ליצור אותה בלי לצאת מעמוד המתכונים.
- נוסף popup נוסף לניהול הופעת המתכון בספרים שנוצרו עם AI. הוא כולל חיפוש חופשי לפי שם, קטגוריות ותגיות, ומאפשר להוסיף, להסיר או להעביר מתכון בין ספרי AI בלי לפתוח כל ספר בנפרד.
- שורת הפעולות בכרטיסי desktop/mobile הוגדרה כרצועת אייקונים קבועה שאינה מתכווצת. גם לאחר הוספת פעולות רשימת הקניות והספרים נשארים גלויים תפריט שלוש הנקודות, דירוג, העתקה ושאר האייקונים.
- שמות מתכונים חדשים עוברים בדיקת ייחודיות בקבוצה. במקרה של שם קיים, המערכת מנסה קודם להוסיף ייחוס קצר של שף/יוצר/מקור, ואם עדיין קיימת כפילות היא מוסיפה מספר עולה בלי לדרוס מתכון קיים.
- מחיקת מתכון מציגה מראש רשימות קניות ואתרי קניות המקושרים אליו. המשתמש יכול לבחור כל פריט מקושר בנפרד; ברירת המחדל מוחקת רק את המתכון ומשאירה את הישויות המקושרות.
- נוסף toggle מהיר להצגה/הסתרה של תמונות מצרכים בסרגל המתכונים וליד כותרת "מרכיבים" בדף המתכון. ההעדפה נשמרת מקומית למשתמש.
- לאחר יצירת תמונות מצרכים למתכון נשמר סימון ב-`extras` של המתכון (`itemImagesEnsured` ו-`itemImagesEnsuredAt`), כדי לא להציג שוב את הכפתור אחרי רענון.
- בכרטיסי המתכונים נוספו גם כפתורי אייקון מהירים להוספה לתוכנית ולהוספה לרשימת קניות, בלי להגדיל את שורת הפעולות ובלי להסיר את תפריט שלוש הנקודות.
- כפתור רשימת הקניות הגלוי בעמוד המתכון פותח מיד רשימת קניות קיימת בשם המתכון, או יוצר רשימה חדשה ומנווט אליה. אם ספק AI פעיל, יצירת הרשימה החדשה מסדרת אותה עם AI כברירת מחדל.
- כפתור רשימת הקניות הגלוי בכרטיס מתכון פותח את חלון "הוסף לרשימת קניות", כדי לבחור רשימה או ליצור רשימה חדשה בלי לעזוב את רשימת המתכונים.
- חלון "פתח/צור רשימת קניות" בכרטיס מתכון נפתח כעת כברירת מחדל במצב תצוגה נקי ומקובץ לפי קטגוריות, עם התקדמות ופריטים שסומנו. כפתור עריכה באותו popup עובר לממשק העריכה המלא הקיים וחזרה לתצוגה, בלי לפתוח דף או חלון נוסף.
- מצב התצוגה של חלון רשימת הקניות מאפשר לסמן ולבטל סימון של פריטים ישירות, בלי לעבור למצב עריכה. כותרות הקטגוריות בחלון, בפתיחה המהירה ממסך הרשימות ובעמוד הרשימה המלא משתמשות באותו צבע אחיד ומציגות ספירת פריטים חיה; הספירה נגזרת מהפריטים ולכן מתעדכנת גם ברשימות מאוחדות.
- סדר הקטגוריות ברשימת קניות נשאר קבוע בכל התצוגות. סימון ב-V מזיז רק את הפריט לתחתית הקטגוריה שלו ואינו מסדר מחדש את הקטגוריות עצמן.
- בתפריט שלוש הנקודות של מתכון נוספה פעולה "צור רשימת קניות עם AI". הפעולה יוצרת רשימת קניות חדשה משם המתכון, מוסיפה את רכיבי המתכון, ומסדרת אותה עם AI כולל הערות וטיפים.
- בכרטיס מתכון שאין לו רשימת קניות מקושרת מופיע אייקון ירוק במרכז החלק העליון של אזור התמונה. לחיצה פותחת את חלון הרשימה המהיר, יוצרת ומסדרת רשימה עם AI ומציגה מצב טעינה והודעת הצלחה/כשל. אחרי יצירה מוצלחת האייקון נעלם; הבדיקה אם קיימת רשימה נעשית בשאילתות bulk מוגבלות לעמוד המתכונים הנוכחי ולא בבקשה נפרדת לכל כרטיס.
- בתפריט שלוש הנקודות של כרטיס מתכון נוספה פעולה "הוסף תמונה עם AI". בכרטיסים שאין להם תמונה, או שהתמונה שלהם שבורה, מופיע גם כפתור צף קטן בפינה הימנית העליונה של אזור התמונה שמפעיל את אותה פעולה בלי להזיז את שלוש הנקודות, הכוכבים או שורת האייקונים.
- יצירה עם AI יכולה עכשיו להביא ולשמור גם תמונות לרכיבים, לכלים ולפריטי רשימת קניות. ברירת המחדל פעילה, ויש checkbox בחלון "יצירה עם AI" ובתוסף הדפדפן כדי לכבות זאת לפני יצירה.
- תמונות פריטים נשמרות בקאש משותף לפי קבוצה, סוג פריט ושם מנורמל, כך שאם כבר קיימת תמונה ל"עגבניה" או "מלפפון" משתמשים בה שוב במקום להוריד כפילויות.
- ה-AI משמש כדי לבנות שאילתת חיפוש תמונה באנגלית לפי שם הפריט והקשר המתכון, ולאחר מכן המערכת מנסה להוריד תמונה ציבורית, למזער אותה ל-WebP ולשמור אותה מקומית.
- במתכון נוסף כפתור ליד כותרת המרכיבים להצגה/הסתרה של תמונות רכיבים וכלים. ההעדפה נשמרת למשתמש.
- ברשימות קניות נוסף כפתור להצגה/הסתרה של תמונות פריטים, גם בעמוד רשימת קניות מלא וגם בפתיחה המהירה של רשימות מהמסך הראשי.
- סידור רשימת קניות עם AI מנסה להביא תמונות לפריטים אחרי הסידור, תוך שימוש באותו קאש כדי לא לחזור על הורדות.
- אם כבר קיימת רשימת קניות בשם המתכון, הפעולה יוצרת שם פנוי עם מספר בסוגריים כדי לא לדרוס רשימה קיימת.
- לכפתורי האייקון המהירים בכרטיסי המתכונים נוסף דגש hover/focus ברור, בלי לשנות את גודל הכרטיס או להזיז רכיבים.
- פעולת "העתק מתכון" הוסרה מתפריט שלוש הנקודות בכרטיס כדי למנוע כפילות, אבל "שיתוף" נשאר בתפריט שלוש הנקודות.
- חלון שיתוף המתכון הורחב עם אפשרויות שיתוף מהירות: שיתוף מערכת/דפדפן, WhatsApp, אימייל והעתקת קישור. השיתוף כולל את המרכיבים ואופן ההכנה, בנוסף לקישור השיתוף.
- חלון "הוסף לרשימת קניות" מציג את רשימת הרכיבים מיד באותו חלון, בלי שלב "הבא", ועדיין מאפשר לבחור רשימה קיימת/חדשה ולהוסיף פריטים ידניים.
- חלון "הוסף לרשימת קניות" צומצם לרוחב נוח יותר, והרכיבים מוצגים כרשימה קומפקטית במקום פריסה רחבה עם שטח ריק.
- בתוך חלון "הוסף לרשימת קניות" ניתן לערוך שם/טקסט של רכיב לפני השמירה, או למחוק רכיב מהרשימה בעזרת אייקון פח.
- לחיצה על "מתכונים" בסרגל הצד מאפסת במפורש את סינוני החיפוש האחרונים ומחזירה לתצוגת כל המתכונים.
- הניווט המהיר כולל עכשיו גם רשימות קניות, כדי שאפשר לעבור בין רשימות קניות מהצד גם כשכבר נמצאים בתוך רשימה.
- ברשימת קניות, סימון פריט ב-V כבר לא מסתיר אותו באזור נפרד. הפריט נשאר במקום עם קו חוצה, ואזור המסומנים משמש רק לפעולות כמו ביטול סימון הכל או מחיקת מסומנים.
- ברשימת קניות, פריטים שסומנו ב-V יורדים לתחתית תת-הקטגוריה שלהם, כך שלמעלה נשארים תמיד הפריטים שעדיין לא סומנו.
- תצוגת התוויות ברשימת קניות הוחלפה מפאנלים מתקפלים לכותרות ביניים בתוך רשימה אחת רציפה, כדי שהקטגוריות לא יתנהגו כפריטים נפרדים שאפשר למזער.
- ברשימת קניות, פריט שסומן ב-V כבר לא מציג מתחתיו תאריך "הושלם ב"; נשאר רק קו חוצה נקי.
- בעמוד רשימת קניות נוסף כפתור "העתק" גלוי ליד פעולות הרשימה, כדי להעתיק את הרשימה כטקסט ולשלוח בוואטסאפ בלי לפתוח תפריט נוסף.
- במסך הראשי של רשימות הקניות נוסף כפתור העתקה על כל כרטיס רשימה, כך שאפשר להעתיק רשימה בלי להיכנס לתוכה.
- בניווט המהיר של רשימות הקניות נוסף כפתור העתקה ליד כל רשימה, עם טעינה נקודתית בזמן שליפת הרשימה המלאה.
- במסך הראשי של רשימות הקניות נוסף כפתור עריכה על כל כרטיס רשימה, כדי לשנות שם רשימת קניות בלי להיכנס לתוכה.
- בעמוד רשימת קניות נוסף "מצב AI" עם צ'יפ שמראה אם הרשימה כבר עברה סידור AI או לא.
- נוספה פעולה "סדר עם AI" שמנתחת את פריטי רשימת הקניות ומסדרת אותם לתוויות/קטגוריות שימושיות כמו ירקות ופירות, מוצרי חלב וביצים, מזווה ויבשים, תבלינים ורטבים ושונות.
- אם ה-AI מחזיר קטגוריה שלא קיימת, המערכת יוצרת תווית חדשה ומחברת אותה לרשימות הקניות דרך מנגנון התוויות הקיים.
- סטטוס הסידור נשמר ב-`extras` של רשימת הקניות (`aiOrganized` ו-`aiOrganizedAt`), בלי migration חדש.
- סידור רשימת קניות עם AI נחסם לרשימה ריקה גם בממשק וגם בצד השרת, כדי למנוע קריאת AI מיותרת או סימון שגוי של רשימה ריקה.
- במסך הראשי של רשימות הקניות נוסף צ'יפ AI לכל רשימה וכפתור מהיר לסידור הרשימה עם AI בלי להיכנס לתוכה.
- כותרת עמוד רשימת הקניות הוגדלה וקורבה לקו המפריד, כדי שהשם יהיה ברור יותר.
- אזור הפעולות בראש רשימת הקניות הועבר לשורה נפרדת ומיושר שמאלה מתחת לאיור העגלה, כדי שלא יעלה על אייקון רשימת הקניות במסכים רחבים או צפופים.
- במסך הראשי של רשימות הקניות נוספה העדפה "פתח רשימות כאן": כברירת מחדל לחיצה על רשימה פותחת אותה מתחת לכרטיס בלי מעבר עמוד, ועדיין אפשר לכבות את זה ולחזור להתנהגות הישנה.
- הפתיחה המהירה של רשימת קניות טוענת את פריטי הרשימה רק בלחיצה על אותה רשימה, כדי לא להעמיס מראש על דף שיש בו הרבה רשימות.
- בפתיחה המהירה של רשימת קניות ניתן לערוך טקסט של פריט או למחוק פריט בלי להיכנס לדף הרשימה המלא.
- ברשימת קניות נוספה פעולה לסימון "כל הקניות קיימות"; הסטטוס נשמר ב-`extras` של הרשימה (`allGroceriesReady` ו-`allGroceriesReadyAt`) בלי migration חדש.
- לפעולת "כל הקניות קיימות" נוסף דגש hover/focus וכפתור איפוס נפרד, גם בדף רשימת קניות מלא וגם בפתיחה המהירה, כדי שיהיה ברור מתי המצב הופעל ומתי מאפסים אותו.
- כאשר שם רשימת קניות שסומנה כ"כל הקניות קיימות" זהה לשם מתכון, עמוד המתכון וכרטיסי המתכון מציגים סימון ירוק שמראה שכל המרכיבים זמינים.
- במסך רשימות הקניות נוסף חיפוש מהיר לפי שם רשימה. חיפוש של רשימת מקור מציג גם את הרשימה המאוחדת שאליה היא משויכת ופותח את רשימות המקור מתחתיה.
- נוסף כפתור "איחוד רשימות" שבוחר שתי רשימות או יותר ויוצר רשימה מאוחדת חדשה, בלי לשנות או למחוק את רשימות המקור.
- פריטים מובנים בעלי אותו מזון ויחידות תואמות מתאחדים לכמות אחת, כולל המרת יחידות דרך מנגנון היחידות של Mealie; לדוגמה `300 גרם + 500 גרם = 800 גרם`.
- קטגוריית/תווית הפריט נשמרת בזמן האיחוד, ומזון זהה אינו מוכפל רק מפני שהגיע משתי רשימות שונות.
- רשימה מאוחדת שומרת ב-`extras` את מזהי ושמות רשימות המקור. במסך הראשי ניתן לפתוח ולמזער את רשימות המקור מתחת לרשימה המאוחדת, ולחיצה על מקור פותחת את הרשימה המקורית עצמה.
- ביטול איחוד מוחק רק את הרשימה המאוחדת. רשימות המקור והסימונים שלהן נשארים ללא שינוי וחוזרים מיד לרשימה הראשית.
- נוסף כפתור "מחק הכל" שפותח חלון בחירה שבו כל הרשימות מסומנות כברירת מחדל; אפשר להסיר V מכל רשימה שרוצים להשאיר לפני המחיקה.
- שמות ארוכים בכרטיסי רשימות קניות מוגבלים לשתי שורות עם חיתוך, ואזור הפעולות נשמר ברוחב עצמאי כדי שאייקוני עריכה, העתקה, AI, בעלים ומחיקה יישארו זמינים.
- מסך רשימות הקניות כולל מיון רב-רמתי דמוי Excel לפי תאריך יצירה, שם או מספר פריטים, בכל אחד מהכיוונים עולה/יורד.
- אפשר לבחור אם רשימות מאוחדות יופיעו ראשונות, אחרונות או כחלק מהמיון הרגיל, ואם כלל הרשימות המאוחדות הוא רמת המיון הראשונה או השנייה.
- מספר הפריטים מוחזר בתקציר כל רשימה דרך שאילתת count במסד הנתונים, בלי לטעון את כל פריטי הרשימה רק כדי לבצע מיון.
- הוקשח איחוד פריטי טקסט ישנים שלא עברו parsing: פריטים שונים ללא `foodId` אינם מתאחדים בטעות רק מפני ששניהם חסרים מידע מובנה.
- דירוג מהיר של מתכון בתפריט שלוש הנקודות הועבר לסוף הרשימה, כדי ששאר הפעולות הרגילות יישארו ראשונות.
- כרטיסי מתכונים בתצוגת הרשת קיבלו גובה אחיד: אזור הכותרת שמור תמיד לעד 3 שורות, ושורת הפעולות נשארת בגובה קבוע.
- סרגל הפעולות הכחול בעמוד המתכון הוחזר לתצוגה מלאה וקיבל כפתור "עריכה מהירה". במצב זה ניתן לערוך במקום את שם המתכון, המרכיבים, אופן ההכנה וההערות, ולשמור או לבטל בלי לעבור למסך העריכה המלא.
- בכרטיסי מתכון נוסף אייקון עריכה מהירה שפותח popup מצומצם עם אותם שדות בסיסיים. החלון נטען רק בלחיצה ומתנקה בסגירה, ושורת האייקונים ממשיכה להציג גם את שאר הפעולות ואת תפריט שלוש הנקודות.
- תגיות בכרטיסי מתכון מוצגות בשורה ייעודית אחת מתחת לשם המתכון. אם יש יותר תגיות ממה שנכנס בשורה, רק מה שנכנס מוצג והשאר נחתך ויזואלית.

קבצים מרכזיים:

- `frontend/app/components/Domain/Recipe/RecipeContextMenu/RecipeContextMenu.vue`
- `frontend/app/components/Domain/Recipe/RecipeContextMenu/RecipeContextMenuContent.vue`
- `frontend/app/components/Domain/Recipe/RecipeDialogAddToShoppingList.vue`
- `frontend/app/components/Domain/Recipe/RecipeDialogShare.vue`
- `frontend/app/components/Domain/Recipe/RecipeActionMenu.vue`
- `frontend/app/components/Domain/Recipe/RecipeCard.vue`
- `frontend/app/components/Domain/Recipe/RecipeCardMobile.vue`
- `frontend/app/components/Domain/Recipe/RecipeCardSection.vue`
- `frontend/app/components/Domain/Recipe/RecipeExplorerPage/RecipeExplorerPage.vue`
- `frontend/app/components/Domain/ItemImages/ItemImageThumb.vue`
- `frontend/app/components/Domain/Recipe/RecipeIngredients.vue`
- `frontend/app/components/Domain/ShoppingList/ShoppingListItem.vue`
- `frontend/app/composables/recipes/use-recipe-copy.ts`
- `frontend/app/composables/recipes/use-recipe-shopping-list-copy.ts`
- `frontend/app/composables/recipes/use-recipe-item-images.ts`
- `frontend/app/components/Layout/DefaultLayout.vue`
- `frontend/app/components/Layout/LayoutParts/AppOrganizerSidebar.vue`
- `frontend/app/composables/use-recipe-explorer-search.ts`
- `frontend/app/composables/use-users/preferences.ts`
- `frontend/app/composables/shopping-list-page/use-shopping-list-availability.ts`
- `frontend/app/types/application-types.ts`
- `frontend/app/pages/shopping-lists/index.vue`
- `frontend/app/pages/shopping-lists/[id].vue`
- `mealie/services/item_image_service.py`
- `mealie/routes/media/media_item_images.py`
- `mealie/routes/recipe/recipe_crud_routes.py`
- `mealie/services/recipe/recipe_service.py`
- `frontend/app/lib/api/user/group-shopping-lists.ts`
- `mealie/routes/households/controller_shopping_lists.py`
- `mealie/services/household_services/shopping_lists.py`
- `mealie/schema/openai/shopping_list.py`
- `mealie/services/openai/prompts/shopping-lists/organize-shopping-list.txt`

## שמירת אתרי קניות מזון

- מתחת ל"רשימות קניות" נוסף מסך "אתרים" לשמירת חנויות, מסעדות, יצרנים ואתרים שמהם קונים או מזמינים מזון.
- ניתן לשמור אתר ידנית דרך popup עם שם האתר, כתובת, המזון שמופיע בדף הנוכחי ומזונות נוספים שהאתר מציע.
- מצב "נתח באמצעות AI" מקבל URL, קורא כמות מוגבלת מתוכן הדף ומחזיר את שם העסק, המזון בדף וקטגוריות מזון נוספות בלי להמציא מוצרים שאינם מופיעים במקור.
- רשומה קיימת עם אותה כתובת מתעדכנת במקום ליצור כפילות. הכתובות מנורמלות ל-HTTP/HTTPS ונשמרות ללא fragment.
- מסך האתרים כולל חיפוש, פתיחת האתר בטאב חדש, עריכה ומחיקה.
- בתוסף Chrome/Edge נוסף כפתור עצמאי "שמור אתר" שאינו תלוי בבחירת "מה לחלץ". הוא שולח ל-Mealie את הדף שכבר פתוח בדפדפן ומשתמש בספק ה-AI המוגדר באתר, בלי מפתח API נוסף בתוסף.
- popup התוסף בגרסה `0.2.2` קיבל רוחב מעט גדול יותר וריווח אנכי קומפקטי. כל הגדרות החילוץ נשארו זמינות, וכפתור "שמור אתר" מוצג במלואו בלי scrollbar במצב הרגיל.
- נוסף endpoint ייעודי לדף שכבר חולץ בדפדפן, לצד endpoint לניתוח URL בצד השרת ו-CRUD מלא לאתרים.
- אתרי קניות ניתנים כעת לשיוך למתכונים ולרשימות קניות בקשר רבים-לרבים. חלון שיוך מהיר זמין מכרטיס מתכון, מעמוד המתכון, מכרטיס רשימת קניות ומעמוד הרשימה המלא.
- חלון השיוך כולל חיפוש לפי שם/כתובת/מזון, הצגת אתרים שכבר משויכים והוספה או הסרה מיידית בלי לשכפל אתר במסד.
- מחיקת אתר מציגה מתכונים ורשימות קניות המקושרים אליו. ברירת המחדל מוחקת רק את האתר; מחיקת כל ישות מקושרת דורשת סימון מפורש ונפרד.
- מחיקת רשימת קניות מציגה באותה דרך מתכונים ואתרים מקושרים, וגם שם כל האפשרויות הנלוות כבויות כברירת מחדל.

קבצים מרכזיים:

- `frontend/app/pages/shopping-websites/index.vue`
- `frontend/app/lib/api/user/shopping-websites.ts`
- `browser-extension/mealie-recipe-importer/popup.js`
- `mealie/routes/households/controller_shopping_websites.py`
- `mealie/db/models/household/shopping_website.py`
- `mealie/services/openai/prompts/websites/parse-shopping-website.txt`

## מסעדות

- נוסף מסך `מסעדות` מיד אחרי `אתרים` בניווט הראשי, לצד פעולה נפרדת של `הוספה מהירה של מסעדה` שפותחת popup מכל מסך.
- בתוך מסך המסעדות מופיע אותו popup ליצירה ולעריכה, כך שאפשר לעבוד בלי לעבור לדף נוסף.
- אפשר לשמור מסעדה ידנית עם שם, אתר, סוגי אוכל, כמה כתובות, טלפון, טווח מחיר, תיאור, הערות ומידע מישלן.
- למסעדה נשמרים שני סיווגים עצמאיים: רמת המלצה (`מומלץ מאוד`, `מומלץ`, ניטרלי, `לא מומלץ`, `מאוד לא מומלץ`) ומצב ביקור (`ניסיתי` / `עוד לא ניסיתי`). ברירת המחדל היא `מומלץ` ו`עוד לא ניסיתי`.
- מצב AI מקבל שם מסעדה, כתובת אתר או את שניהם, ומארגן את הפרטים הנתמכים. סוגי האוכל והתיאור נשמרים בעברית; אם הספק החזיר אנגלית, מתבצע תיקון מובנה נוסף לעברית.
- חיפוש המיקום של ה-AI נותן עדיפות לסניפים מאומתים בתל אביב ואחר כך לשאר הארץ. המערכת מנסה לשמור כתובת, דירוג Google, מספר ביקורות וקישור Google Maps בלי להמציא נתון שאינו ודאי.
- כל כתובת בכרטיס המסעדה היא קישור ישיר לחיפוש המיקום ב-Google Maps. גם כשלא התקבל קישור רשמי, נוצר קישור חיפוש בטוח לפי שם המסעדה והכתובת.
- ה-AI אינו רשאי להמציא כתובות, דירוג Google או לסמן מישלן רק בגלל שף מפורסם או סגנון יוקרתי.
- המסך מקבץ את הכרטיסים לפי רמת ההמלצה, כולל חיפוש, שינוי מהיר של המלצה/ביקור, פתיחת האתר בטאב חדש, עריכה ומחיקה.
- כפתור `שמור מסעדה` נוסף גם לתוסף Chrome/Edge. התוסף שולח ברקע את הדף הפתוח ל-Mealie, ולכן הפעולה ממשיכה גם אם popup התוסף נסגר.
- שמירה חוזרת לפי אותו URL או אותו שם מעדכנת את הרשומה הקיימת במקום ליצור כפילות.

קבצים מרכזיים:

- `frontend/app/pages/restaurants/index.vue`
- `frontend/app/components/Domain/Restaurant/RestaurantCreateDialog.vue`
- `frontend/app/lib/api/user/restaurants.ts`
- `mealie/routes/households/controller_restaurants.py`
- `mealie/db/models/household/restaurant.py`
- `mealie/services/openai/prompts/restaurants/parse-restaurant.txt`
- `browser-extension/mealie-recipe-importer/background.js`

## האוכל שיש לי / מזווה

- נוסף מסך מזווה אישי שבו אפשר לשמור מה קיים בבית, כולל שם פריט, כמות אופציונלית, יחידת מידה, קטגוריה והערה.
- ניתן ליצור, לחפש, לערוך ולמחוק פריטי מזווה בלי להיכנס למסכים אחרים.
- נוספה התאמה רגילה בין המזווה למתכונים כדי לראות אילו מתכונים אפשר להכין ומה חסר לכל מתכון.
- נוסף חיפוש בעזרת AI: המשתמש יכול לתאר מה הוא רוצה להכין, וה-AI מקבל רשימה מוגבלת ומסודרת של פריטי המזווה ושל מועמדי המתכונים ומחזיר הצעות מתוך המתכונים הקיימים בלבד.
- הכמויות נשארות אופציונליות. כאשר קיימת כמות היא נשלחת ל-AI כדי לשפר את ההתאמה, אך פריט ללא כמות עדיין משתתף בחיפוש.
- המימוש אינו מוסיף polling או cache גלובלי; נתוני המזווה נטענים רק במסך המזווה ותוצאות AI מוגבלות למספר מועמדים קבוע.

## תיקון bridge לתמונות דרך תוסף הדפדפן

- bridge העלאת תמונה מקישור מאשר מיד שקיבל את הבקשה, לפני טעינת קובצי התרגום, כדי למנוע timeout שגוי כשהתוסף מתעורר ממצב שינה.
- בהתקנה, עדכון או הפעלת הדפדפן התוסף מזריק את ה-bridge ללשוניות Mealie שכבר פתוחות לפי כתובת האתר השמורה.
- החל מגרסה `0.3.4`, ה-bridge מוזרק גם בכל עלייה מחדש של ה-service worker. כך טעינת תוסף unpacked מחדש מתקנת מיד לשוניות Mealie שכבר פתוחות, בלי ליצור listener כפול ובלי להמתין לרענון דף.
- ההזרקה מוגנת מפני רישום כפול של listener; כל timeout, listener ולשונית תמונה זמנית עדיין מתנקים בסיום הפעולה.

## ניווט מהיר לספרים, קטגוריות ותגיות

- נוסף סרגל ניווט משני שנצמד לסרגל הצד הראשי, בלי להחליף את הניווט הקיים.
- הסרגל מותאם לכיוון השפה דרך Vuetify RTL/LTR.
  בעברית הוא יושב בצד הימני/התחלתי של הממשק, ובאנגלית בצד השמאלי/התחלתי.
- הסרגל יכול להציג:
  - רשימות קניות
  - ספרי בישול / Cookbooks
  - קטגוריות
  - תגיות
- ניתן למזער ולהגדיל את הסרגל בלחיצה על אייקון הניווט המהיר.
- מצב המזעור נשמר מקומית בדפדפן, כך שהוא נשאר גם לאחר רענון.
- המשתמש יכול לבחור מתוך כפתור ההגדרות של הסרגל מה להציג ומה להסתיר.
- בתוך הגדרות הניווט המהיר נוספה גרירה ושחרור לשינוי סדר הסעיפים, למשל תגיות לפני ספרים או קטגוריות לפני תגיות.
- סדר הסעיפים נשמר מקומית בדפדפן וממשיך להשפיע גם אחרי רענון.
- ברירת המחדל מציגה ספרי בישול, וניתן להפעיל קטגוריות ותגיות לפי הצורך.
- קטגוריות ותגיות ריקות לא מוצגות בניווט המהיר; הן מופיעות רק אם יש להן לפחות מתכון אחד.
- לכל מקטע פעיל נוספו שדה חיפוש נפרד ואזור גלילה פנימי מוגבל: רשימות קניות, ספרי בישול, ספרים מתורגמים, קטגוריות ותגיות. רשימה ארוכה אינה דוחפת עוד את שאר אזורי הניווט המהיר אל מחוץ למסך.
- לחיצה על כותרת של כל מקטע ממזערת או פותחת אותו. מצב הפתיחה של כל מקטע נשמר מקומית בדפדפן ונשאר לאחר רענון.
- חיפוש במקטע שמכיל קבוצות מקוננות בודק גם את שמות פריטי המשנה ולא רק את כותרת הקבוצה.
- לחיצה על ספר פותחת את עמוד ה-Cookbook.
- לחיצה על קטגוריה או תגית מסננת את עמוד המתכונים לפי אותו פריט.
- דף המתכונים עודכן כך ששינוי query מה-sidebar מסנכרן את החיפוש גם כשהמשתמש כבר נמצא בדף המתכונים, בלי צורך ברענון.
- הניווט הראשי מציג את כל אזורי המערכת גם למשתמש מנותק, כדי שאפשר יהיה להבין אילו יכולות קיימות. פעולה שדורשת חשבון מפנה להתחברות ואינה טוענת מתכונים, רשימות קניות או מידע פרטי לפני ההתחברות.
- הנתיב `/g/home` מזוהה ככינוי לקבוצה של המשתמש המחובר. כך מעבר בין מתכונים, רשימות קניות, מאמרים, תכנון ארוחות וספרי בישול נשאר באותו הקשר ולא מנסה לטעון בטעות קבוצה ציבורית בשם `home`.

קבצים מרכזיים:

- `frontend/app/components/Layout/LayoutParts/AppOrganizerSidebar.vue`
- `frontend/app/components/Layout/LayoutParts/AppSidebar.vue`
- `frontend/app/components/Layout/DefaultLayout.vue`
- `frontend/app/composables/use-users/preferences.ts`
- `frontend/app/composables/use-recipe-explorer-search.ts`
- `frontend/app/types/application-types.ts`
- `frontend/app/lang/messages/he-IL.json`
- `frontend/app/lang/messages/en-US.json`

## ספריית ספרים ויצירת ספרי בישול עם AI

- עמוד "ספרי בישול" ממשיך להציג את ספרי הסינון החכמים של Mealie, ובנוסף כולל ספרייה חזותית של קבצים שהועלו, ספרים מתורגמים וספרים שנוצרו עם AI.
- בספרייה ניתן לחפש לפי שם/קובץ/תקציר ולסנן לפי סוג ספר, מטבח, רמה, קטגוריות ותגיות.
- בזמן העלאת ספר ניתן לבחור אם לסווג אותו עם AI. הסיווג שולח רק את שם הספר ושם הקובץ, ולא את הספר המלא או עמודים מתוכו, ומחזיר בין השאר:
  - מטבח וסוג ספר.
  - רמת קושי ורמת לימוד.
  - מחבר/שף ומסעדה.
  - טכניקות, קטגוריות, תגיות ושפה.
  - קשר למישלן רק כאשר יש לכך תמיכה בטקסט, בלי להמציא שיוך.
- ניתן לסווג או לרענן סיווג גם אחרי ההעלאה מתוך כרטיס הספר.
- הסיווג שומר גם תקציר ספר, מטבח, סוג, רמת קושי/לימוד, טכניקות, קטגוריות ותגיות, ומנסה לשמור עטיפה מקומית. אם אין עטיפה ישירה הוא מחפש תמונה רלוונטית למחבר, לשף או לנושא הספר בלי להחזיק את קובץ הספר בזיכרון.
- בעמוד המתכונים נוסף כפתור "צור ספר עם AI". המשתמש בוחר או הגדרה מוכנה, למשל אוכל איטלקי/ארוחות ערב/אפייה, או הנחיה חופשית; שני המצבים בלעדיים.
- ה-AI מחפש בכל אינדקס המתכונים בקבוצה במנות מוגבלות, בוחר רק slugs קיימים, בונה פרקים ותוכן עניינים ומפיק ספר HTML מלא עם רכיבים, אופן הכנה ומקור.
- תכנון הפרקים נשלח ל-AI במנות של עד 80 מתכונים כדי לא ליצור prompt יחיד גדול מדי. כל מתכון מתאים נשמר בדיוק פעם אחת, ומתכונים שה-AI לא שייך לפרק נכנסים לפרק fallback לפי הקטגוריה שלהם.
- ספר ארוך מתפצל אוטומטית ל-`Vol. 1`, `Vol. 2` וכן הלאה לפי מגבלת מספר מתכונים ומספר עמודים משוער שהמשתמש יכול לשנות.
- כפתור "רענון" בספר AI מריץ שוב את החיפוש, שומר מתכונים שכבר נכללו ומוסיף מתכונים חדשים שמתאימים להגדרה. אם מספר הכרכים השתנה, כרכים עודפים ישנים נמחקים בצורה מבוקרת.
- ספרים שנוצרו עם AI נשמרים כקובצי Uploaded Book רגילים, ולכן ניתן לפתוח, לחפש, לסנן ולמחוק אותם מאותה ספרייה.
- מחיקת ספר מציגה preview של המתכונים ורשימות הקניות המשויכים אליו. ברירת המחדל משאירה אותם; המשתמש יכול לבחור למחוק מתכונים ורשימות קניות בנפרד, ומחיקת מתכון או רשימה לעולם אינה מוחקת את הספר עצמו.

## חילוץ מתכונים מספרים עם AI

- נוסף עיבוד AI לספרים שהועלו למערכת.
- ניתן להפעיל חילוץ מתכונים:
  - בזמן העלאת ספר, באמצעות סימון "חלץ מתכונים עם AI אחרי ההעלאה".
  - אחרי שהספר כבר עלה, מתוך פעולת "חלץ מתכונים עם AI" תחת הספר בניווט.
- ברירת המחדל היא חלוקה ל-10 עמודים בכל chunk, והמשתמש יכול לבחור מספר אחר בין 1 ל-100.
- ניתן לבחור טווח עמודים לחילוץ: "מעמוד" ו"עד עמוד".
  אם משאירים את שני השדות ריקים, החילוץ רץ על כל הספר.
- טווח העמודים האחרון שנבחר לחילוץ נשמר במסד ומוצג בחלון החילוץ ובמשימות הרקע, כדי לדעת מאיזה עמוד עד איזה עמוד כבר הופעל חילוץ.
- אם ספר נכשל חלקית ומפעילים שוב עם אותו טווח עמודים, המערכת ממשיכה את החלקים שנכשלו; אם משנים טווח, מתחילה ריצה חדשה לטווח החדש.
- העיבוד רץ ברקע, כדי שהדפדפן לא ייתקע בזמן עיבוד ספר ארוך.
- לכל ספר נשמר סטטוס חילוץ:
  - לא התחיל
  - בעיבוד
  - הסתיים
  - נכשל
- נשמרים גם מונים:
  - מספר chunks כולל
  - chunks שהושלמו
  - מתכונים שנמצאו
  - מתכונים שנשמרו
  - הודעת שגיאה אחרונה/חלקית
- בזמן עיבוד, רשימת הספרים מתרעננת אוטומטית כל כמה שניות ומציגה התקדמות ליד שם הספר.
- בכרטיס של כל ספר הועברה פעולת החילוץ ל-popup מלא. המשתמש יכול לבחור את כל הספר או טווח עמודים, גודל chunk, יצירת רשימות קניות מסודרות עם AI והאם לאפשר יצירה נוספת כאשר כבר קיימים מתכון או רשימה מאותו מקור.
- החילוץ תומך כרגע בפורמטים שניתן לחלץ מהם טקסט ישירות:
  - PDF
  - EPUB
  - TXT / Markdown
  - HTML / MHTML
  - DOCX
  - ODT
  - FB2 / FB2.ZIP
  - RTF בסיסי
- קבצי PDF עוברים חילוץ טקסט באמצעות `pypdf`.
- ה-AI מקבל כל chunk ומחזיר רשימת מתכונים במבנה מסודר, לא טקסט חופשי.
- פרומפט החילוץ דורש מעבר עמוד-עמוד על כל הכנה בעלת כותרת, ולא רק על מנות עיקריות. גם צירים, רטבים, jus, בצקים, צורות פסטה, חמאות, מילויים וקישוטים קצרים נשמרים כמתכון כשהספר מספק רכיבים ושיטת הכנה.
- המתכונים מתורגמים לעברית כחלק מהחילוץ.
- מתכונים שנמצאו נשמרים כמתכוני Mealie רגילים, דרך אותו מנגנון שכבר משמש ל"יצירה עם AI".
- מקור המתכון נשמר לפי שם הספר וטווח העמודים אם ה-AI לא מצא מקור מפורש.
- מתכון שחולץ מספר שומר ב-`extras` גם את מזהה הספר ואת עמוד/טווח המקור. השדה "מקור" בעמוד המתכון הופך לקישור שפותח את הספר ישירות בעמוד המתאים.
- אם קיימת גרסה מתורגמת שהושלמה וכוללת את העמוד המבוקש, הקישור נפתח בה ב-`#page-N`; אחרת PDF המקור נפתח ב-`#page=N`. מתכונים ישנים ללא מזהה ספר מקבלים fallback שמאתר את הספר לפי שם המקור והעמוד שכבר שמורים בטקסט.
- בספרייה ובניווט המהיר נוספה פעולה "מחיקת מתכונים מהספר", הזמינה גם מספר המקור וגם מהעותק המתורגם שלו. החלון טוען רק מתכונים המשויכים לספר המקור ולמשק הבית הנוכחי, מסמן את כולם למחיקה כברירת מחדל, ומאפשר לחפש ולהסיר סימון ממתכונים שרוצים להשאיר. המחיקה מתבצעת בפעולת bulk אחת דרך `RecipeService.delete_many`, כולל בדיקות הרשאה וניקוי נכסי המתכון.
- אם ספק ברירת המחדל הוא Google Gemini ויש בו כמה API keys, כל מפתח הופך ל-slot נפרד והעיבוד מחולק ביניהם במקביל. מספר הקריאות במקביל נקבע לפי מספר המפתחות הזמינים וכמות ה-chunks בספר, בלי cap קבוע של 6.
- אם ספק AI מחזיר rate limit / quota / retry-after, המפתח הספציפי נכנס ל-cooldown, ה-chunk חוזר לתור, והמערכת מנסה להמשיך עם מפתח אחר.
- לכל chunk נשמר סטטוס JSON במסד: ממתין, בעיבוד, retry, הושלם או נכשל, כולל מספר ניסיונות, ספק אחרון, שגיאה אחרונה וכמות מתכונים שנשמרו.
- ספר לא מסומן כ"הושלם" אם נשארו chunks שלא עברו. במקרה כזה הוא מסומן כ-`partial_failed`, ואפשר להפעיל חילוץ שוב כדי לנסות להשלים רק את החלקים שלא הושלמו.
- נוסף retry מדורג לכל chunk, עד 6 ניסיונות, עם שימוש בזמן המתנה שהספק מחזיר אם קיים.
- נוסף מנגנון מניעת כפילויות לפי שם מתכון + מקור/טווח עמודים, כדי ש-retry לא ייצור עותקים כפולים של אותו מתכון.
- בחילוץ מתכונים בלבד, כל chunk מקבל עד שני עמודים עוקבים כהקשר חופף. ה-AI מחזיר רק מתכונים שמתחילים בטווח הראשי ומשתמש בעמודי החפיפה רק כדי להשלים מתכון שחוצה גבול בין chunks. טווח המקור והתקדמות ה-chunk נשארים לפי העמודים הראשיים, ותרגום ספרים ממשיך ללא חפיפה כדי לא לשכפל עמודים.
- בדיקת הכפילויות בחידוש חילוץ הוקשחה כך שמתכון בעל אותו שם מאותו ספר מזוהה גם אם טווח העמודים שנרשם במקור השתנה מעט בין ניסיונות.
- כל מתכון שחולץ נשמר עם `uploadedBookSourceId` וטווח העמודים ב-`extras`, גם אם המודל החזיר ניסוח מקור אחר. כך העשרה מאוחרת, מחיקה מרוכזת וקישור חזרה לעמוד הספר אינם תלויים בהשוואת טקסט שבירה.
- שלב ההעשרה אחרי חילוץ משתמש באותו pipeline של יצירה עם AI: קטגוריות, תגיות, מקור ספר עקבי, תמונת מתכון ורשימת קניות אופציונלית. כשל בהורדת תמונה אינו מבטל מתכון תקין.
- בחלון מחיקת מתכונים מספר, מחיקת המתכונים ומחיקת רשימות הקניות שלהם מסומנות כברירת מחדל. אפשר לבטל כל סוג מחיקה בנפרד וגם להסיר V ממתכונים שרוצים להשאיר.
- אם ספק ברירת המחדל אינו Gemini, העיבוד עובד דרך ספק אחד.

קבצים מרכזיים:

- `mealie/services/uploaded_books/book_recipe_extractor.py`
- `mealie/routes/households/controller_uploaded_books.py`
- `mealie/db/models/household/uploaded_book.py`
- `mealie/schema/cookbook/uploaded_book.py`
- `mealie/schema/openai/recipe.py`
- `mealie/services/openai/prompts/recipes/extract-book-recipes.txt`
- `mealie/alembic/versions/2026-07-07-21.20.00_d4e5f6a7b8c9_add_uploaded_book_ai_extraction.py`
- `mealie/alembic/versions/2026-07-07-22.10.00_e7f8a9b0c1d2_add_uploaded_book_extraction_retries.py`
- `mealie/alembic/versions/2026-07-08-04.00.00_d7e8f9a0b1c2_add_uploaded_book_page_ranges.py`
- `frontend/app/components/Layout/DefaultLayout.vue`
- `frontend/app/lib/api/user/uploaded-books.ts`
- `frontend/app/lib/api/types/uploaded-book.ts`

## תרגום ספרים עם AI

- נוסף מצב "תרגם ספר עם AI" בזמן העלאת ספר וגם מתוך פעולות ספר שכבר הועלה.
- בזמן העלאה ניתן לבחור פעולה אחת בלבד:
  - רק להעלות את הספר
  - חילוץ מתכונים עם AI
  - תרגום ספר עם AI
- לא ניתן להפעיל חילוץ מתכונים ותרגום ספר במקביל על אותו ספר. כאשר בוחרים בחלון התרגום גם חילוץ, המערכת מריצה קודם את החילוץ עד לסיומו ורק אחר כך מתחילה את התרגום, כדי שהמתכונים ורשימות הקניות יוכלו להשתלב בספר המתורגם בלי שתי עבודות מתחרות על אותו קובץ.
- שפת ברירת המחדל לתרגום נקבעת לפי שפת האתר של המשתמש.
- ניתן לבחור שפה מתוך dropdown או להקליד שפה אחרת ידנית.
- גם בתרגום ספרים ניתן לבחור כמה עמודים לשלוח בכל chunk, ברירת מחדל 10 עמודים.
- ניתן לבחור טווח עמודים לתרגום: "מעמוד" ו"עד עמוד".
  אם משאירים את שני השדות ריקים, התרגום רץ על כל הספר.
- טווח העמודים האחרון שנבחר לתרגום נשמר במסד ומוצג בחלון התרגום ובמשימות הרקע, כדי לדעת מאיזה עמוד עד איזה עמוד כבר תורגם.
- אם תרגום נכשל חלקית ומפעילים שוב עם אותה שפה, אותו גודל chunk ואותו טווח עמודים, המערכת ממשיכה את החלקים שנכשלו; שינוי טווח מתחיל ריצה חדשה לטווח החדש.
- מנגנון התרגום משתמש באותו retry/cooldown של חילוץ המתכונים:
  - Gemini multi-key
  - המתנה לפי retry-after/rate limit
  - retry מדורג לכל chunk
  - סטטוס לכל chunk
- כל chunk מתורגם ונשמר זמנית כקובץ JSON פנימי.
- כאשר כל ה-chunks הושלמו, הספר המתורגם נבנה ומסומן כהושלם.
- אם חלקים נכשלו, כל העמודים שכן תורגמו נשמרים ונבנה גם ספר חלקי שמיש. הסטטוס נשאר `partial_failed`, הספר מציג במפורש את טווחי העמודים החסרים, וניסיון חוזר מעבד רק את החלקים שלא הושלמו.
- ביקורת השלמות ממשיכה לבדוק את כל העמודים שהוחזרו ולפסול עמודים חשודים, עודפים או מקוצרים. במצב חלקי היא מתעדת את העמודים החסרים במקום למחוק את העבודה התקינה שכבר נשמרה.
- ניתן להשלים עמוד חסר בניסיון חוזר אוטומטי או בהזנה ידנית, ולאחר שכל הטווח הושלם הספר נבנה מחדש ומקבל סטטוס מלא.
- התוצר הוא ספר HTML מתורגם ומעוצב שנפתח בדפדפן מתוך Mealie, עם שער, עטיפה, עמודים ממוספרים ותוכן עניינים מקושר.
- תוכן העניינים אינו עוד רשימה אוטומטית של כל העמודים. אם לספר המקורי יש תוכן עניינים, המערכת משמרת את שמות הפרקים/הסעיפים/המתכונים, ההיררכיה ומספרי העמודים המודפסים שלו ומקשרת אותם לעמוד הפיזי הנכון בתוצר. אם אין תוכן עניינים שמיש, מטא־דאטה מובנה מהתרגום משמש לבניית תוכן עניינים מקצועי של פרקים, סעיפים ומתכונים בלבד.
- גם תרגום של טווח עמודים מקבל תוכן עניינים מסודר עבור הכותרות שנמצאות בטווח, בלי להציף את הניווט בפריטי `עמוד N` חסרי משמעות.
- פרומפט התרגום אוסר במפורש קיצור, סיכום או השמטה. אחרי כל תשובת AI מתבצעת ביקורת של מספר העמודים, יחס הטקסט, מבנה הפסקאות, פריטי רשימות/הוראות וכמויות מספריות; עמוד חשוד נשלח לתיקון מלא, והספר אינו נבנה אם הביקורת עדיין נכשלת.
- בחלון התרגום ניתן לבחור אם להציג בתוך הספר המתורגם את המתכונים שכבר חולצו ממנו באתר. כאשר האפשרות פעילה, כל מתכון משובץ ליד עמוד המקור שלו עם מרכיבים, אופן הכנה, הערות ורשימת קניות קיימת, לצד קישורים ישירים למתכון ולרשימת הקניות ב־Mealie.
- ניתן לבקש בחלון התרגום לחלץ תחילה מתכונים מהטווח, ליצור להם תמונות ורשימות קניות ולסדר את הרשימות עם AI, ורק אחר כך לבנות את הספר המתורגם עם הקישורים. האפשרות זמינה בזמן העלאה וגם מכרטיס של ספר קיים בספרייה.
- נוסף סרגל תוכן עניינים קבוע בצד שמאל של הספר. הסרגל זמין לאורך כל הקריאה, מדגיש את העמוד הפעיל, גולל אוטומטית למיקום הפעיל וניתן למזעור/הרחבה. במובייל הוא מתחיל ממוזער כדי לא להסתיר את הספר.
- נוסף מחוון התקדמות קריאה עצמאי שמציג את מספר העמוד המודפס, מיקום מתוך כלל העמודים ואחוז התקדמות. המחוון מתעדכן לפי הגלילה באמצעות `IntersectionObserver`, ניתן למזעור, וזוכר את בחירת המשתמש בדפדפן.
- לחיצה בתוכן העניינים קופצת מיד לעמוד המבוקש ומעדכנת באותה פעולה את העמוד הפעיל ואת אחוז ההתקדמות.
- מצב הקריאה נשמר כעת לכל משתמש ולכל ספר במסד הנתונים, ולא רק בדפדפן המקומי. נשמרים העמוד והמיקום הנוכחיים, הפרק הפעיל, אחוז הקריאה, הפרקים שסומנו, העדפות התצוגה, הערות ומרקרים.
- לכל פרק בתוכן העניינים נוסף checkbox. סימון פרק מסמן אוטומטית גם את כל הפרקים שלפניו; ניתן לאחר מכן לבטל סימון של פרק מסוים בלי למחוק את שאר ההתקדמות.
- אחוז המיקום הנוכחי בספר ואחוז הפרקים שסומנו מוצגים בנפרד. בכרטיס הספר בספרייה מוצגים שני הנתונים, וספר שסומן כולו מקבל מסגרת ירוקה עדינה וסימון השלמה.
- נוסף פאנל קריאה מתקפל לשינוי גודל וסגנון הגופן, רוחב העמוד, רווח בין שורות ורווח בין מילים. ההעדפות נשמרות ומוחלות שוב בפתיחה הבאה.
- נוסף פאנל מתקפל להערות ולמרקרים. אפשר להציג הכול, רק הערות או רק מרקרים, לקפוץ למיקום המתאים ולמחוק פריט קיים. טקסט משתמש מוצג כטקסט בלבד ואינו מוזרק כ-HTML.
- ספרים מתורגמים נשמרים כ-Uploaded Books רגילים עם סימון `is_translated_book`, וקישור לספר המקורי.
- נוסף סעיף ניווט מהיר "ספרים מתורגמים", שאפשר להפעיל/לכבות ולסדר ב-drag and drop כמו ספרים/קטגוריות/תגיות.
- קבצי HTML שהמשתמש מעלה עדיין מוגשים כהורדה, אבל HTML שנוצר על ידי התרגום מוגש inline כי הוא נבנה ומנוקה על ידי המערכת.

קבצים מרכזיים:

- `mealie/services/uploaded_books/book_recipe_extractor.py`
- `mealie/services/openai/prompts/recipes/translate-book-chunk.txt`
- `mealie/routes/households/controller_uploaded_books.py`
- `mealie/db/models/household/uploaded_book.py`
- `mealie/schema/cookbook/uploaded_book.py`
- `mealie/schema/openai/recipe.py`
- `mealie/alembic/versions/2026-07-08-00.20.00_f1a2b3c4d5e6_add_uploaded_book_translation.py`
- `mealie/alembic/versions/2026-07-08-04.00.00_d7e8f9a0b1c2_add_uploaded_book_page_ranges.py`
- `frontend/app/components/Layout/DefaultLayout.vue`
- `frontend/app/components/Layout/LayoutParts/AppOrganizerSidebar.vue`
- `frontend/app/lib/api/user/uploaded-books.ts`
- `frontend/app/lib/api/types/uploaded-book.ts`

## משימות ברקע וניטור תהליכים

- נוסף כפתור "משימות ברקע" בתפריט הצדדי, עם badge שמציג כמה משימות פעילות כרגע.
- החלון מציג תהליכים ארוכים של ספרים:
  - חילוץ מתכונים מספרים עם AI
  - תרגום ספרים עם AI
- לכל משימה מוצגים:
  - שם הספר
  - סוג הפעולה
  - סטטוס: בעיבוד, ממתין לניסיון חוזר, נכשל חלקית, נכשל או בוטל
  - התקדמות לפי chunks
  - טווח העמודים שעליו המשימה רצה, או "כל הספר" אם לא נבחר טווח
  - כמות כשלים וניסיונות חוזרים
  - הודעת שגיאה אם קיימת
- משימה פעילה שלא עדכנה את רשומת הספר יותר מ-30 דקות מוצגת כ"ייתכן שתקוע", כדי שיהיה קל לזהות תהליכים שלא מתקדמים.
- מתוך החלון ניתן:
  - לפתוח את הספר
  - לעצור משימה פעילה
  - להפעיל ניסיון חוזר למשימה שנכשלה חלקית, נכשלה או בוטלה
  - לרענן ידנית את מצב המשימות
- נוסף סטטוס `cancelled` לחילוץ ולתרגום ספרים.
- עצירת משימה פעילה מתבצעת דרך השרת: הסטטוס מסומן כ-`cancelled`, וה-worker שרץ ברקע בודק את הסטטוס לפני כל chunk חדש ואחרי חזרה מקריאת AI.
- אם חלק כבר נשלח ל-AI בזמן שהמשתמש לוחץ עצור, הוא עשוי לסיים את הקריאה הנוכחית, אבל המערכת לא תמשיך ל-chunk הבא ולא תבנה ספר מתורגם אחרי ביטול.
- מנגנון החידוש האוטומטי של ה-scheduler לא מחזיר משימות שבוטלו, כי `cancelled` אינו חלק מסטטוסי `processing`/`retrying`.

קבצים מרכזיים:

- `frontend/app/components/Layout/DefaultLayout.vue`
- `frontend/app/lib/api/user/uploaded-books.ts`
- `frontend/app/lib/api/types/uploaded-book.ts`
- `mealie/routes/households/controller_uploaded_books.py`
- `mealie/services/uploaded_books/book_recipe_extractor.py`

## שיפורי ממשק ותרגום

- נוספו מחרוזות תרגום בעברית ובאנגלית עבור:
  - יצירה מטקסט
  - העלאת וידאו
  - גודל וידאו / מדיה
  - הגדרות API וספקי AI
  - בדיקת API key
  - הודעות Gemini מרובות מפתחות
  - ניווט מהיר לספרים, קטגוריות ותגיות
- שופר צבע הטקסט של כפתור "יצירה עם AI" בתפריט הצדדי כדי שיהיה קריא.
- נוספה גישה מהירה יותר ליצירה עם AI מטקסט חופשי, בנוסף לתפריט היצירה הרגיל.
- בתפריט שלוש הנקודות של כרטיס מתכון נוספה שורת דירוג מהירה, כך שאפשר לדרג מתכון ישירות מהרשימה בלי לפתוח את עמוד המתכון.
- כותרות מתכונים בכרטיסים מוצגות עד 3 שורות לפני חיתוך עם `...`, כדי ששמות ארוכים יהיו קריאים יותר ברשימות.
- ניווט מהיר מציג קטגוריות ותגיות כברירת מחדל, ומתרענן אחרי יצירת מתכון עם AI כדי שקטגוריות/תגיות שנוצרו או קיבלו מתכון יופיעו מיד בלי רענון ידני של הדף.
- מתחת לספרי בישול נוסף כפתור "העלאת ספר" שפותח popup להעלאת קבצי ספרים, והספרים שהועלו מופיעים בניווט המהיר כקישורים לפתיחה.
- העלאת ספרים תומכת בפורמטים רבים: PDF, EPUB, MOBI, AZW/AZW3/AZW4, FB2/FB2.ZIP, DJVU, CBZ/CBR/CB7/CBT, DOC/DOCX, ODT, TXT, RTF, HTML/MHTML, Markdown, LIT, PDB, ZIP/RAR/7Z.
- ניתן לבחור תיקייה שלמה של ספרים מהדפדפן ולהעלות את כל קבצי הספרים הנתמכים מתוכה. כאשר נבחר יותר מספר אחד, ההעלאה מתבצעת כהעלאה בלבד; חילוץ מתכונים או תרגום זמינים רק לספר יחיד בזמן העלאה או לכל ספר בנפרד אחרי שהוא כבר עלה.
- נוסף כפתור "מחק ספר" בתפריט של ספרים שהועלו. המחיקה מוחקת גם את רשומת הספר וגם את תיקיית הקובץ השמור; אם מוחקים ספר מקור שנוצרו ממנו ספרים מתורגמים, גם הספרים המתורגמים שלו נמחקים כדי לא להשאיר קישורים שבורים.
- תוקנה נפילה בהעלאת ספרים שנגרמה מיצירת רשומת `UploadedBook` בלי להעביר `session` למודל SQLAlchemy של Mealie.
- תוקן חוסר התאמה בין `id` הרשומה במסד לבין תיקיית הקובץ בפועל, כדי שלחיצה על ספר שהועלה תפתח את הקובץ ולא תחזיר `404 Not Found`.

קבצים מרכזיים:

- `frontend/app/lang/messages/he-IL.json`
- `frontend/app/lang/messages/en-US.json`
- `frontend/app/components/Layout/DefaultLayout.vue`
- `frontend/app/components/Layout/LayoutParts/AppSidebar.vue`

## פיתוח והרצה מקומית

- נוספו קבצים להרצת סביבת פיתוח מקומית ומהירה יותר.
- נוספו קבצי Docker Compose מותאמים לפיתוח/hot reload.
- נוסף script להרצת frontend מקומית.
- `docker-compose.custom.yml` מריץ כעת תמונת production מקומית אחת שמכילה גם את Nuxt frontend וגם את ה-API, על `http://localhost:3000`; אין צורך להריץ `npm run dev` בטרמינל נפרד.
- השירות מוגדר עם `restart: unless-stopped`. כאשר Docker Desktop מוגדר לעלות עם Windows, האתר עולה אוטומטית אחרי הכניסה למחשב ולא דורש פקודת frontend או backend נפרדת; ייתכן עיכוב קצר בזמן אתחול Docker.
- תוקן fallback של נתיבי SPA בתמונה המאוחדת: פתיחה ישירה או רענון של נתיב כמו `/g/home` מחזירים את `index.html` בסטטוס `200`, בעוד API או asset שאינם קיימים נשארים `404`.
- עודכנו הגדרות שקשורות לסיומות שורה וקבצי shell כדי להפחית בעיות Windows/Linux.

קבצים מרכזיים:

- `dev-frontend.ps1`
- `docker-compose.custom.yml`
- `docker-compose.dev-hot.yml`
- `.gitattributes`
- `docker/entry.sh`
- `docker/healthcheck.sh`
- `docker/setup_nltk_data.sh`

## API ו-endpoints חדשים/מורחבים

- נוסף endpoint ליצירת מתכון מטקסט:
  - `POST /api/recipes/create/text`
- נוסף endpoint ליצירת מתכון מתוכן שחולץ בדפדפן:
  - `POST /api/recipes/create/browser-page`
- נוספו endpoints להעלאת ספרי בישול כקבצים אמיתיים ושמירתם במערכת:
  - `GET /api/households/uploaded-books`
  - `POST /api/households/uploaded-books`
  - `GET /api/households/uploaded-books/{book_id}/file`
  - `DELETE /api/households/uploaded-books/{book_id}`
  - `POST /api/households/uploaded-books/{book_id}/extract-recipes`
  - `POST /api/households/uploaded-books/{book_id}/extract-recipes/cancel`
  - `POST /api/households/uploaded-books/{book_id}/translate`
  - `POST /api/households/uploaded-books/{book_id}/translate/cancel`
  - `POST /api/households/uploaded-books/{book_id}/classify`
  - `POST /api/households/uploaded-books/generate`
  - `POST /api/households/uploaded-books/{book_id}/refresh-ai`
- נוסף endpoint לייבוא URL מרובה עם קבצי וידאו:
  - `POST /api/recipes/create/url/bulk/assets`
- הורחב endpoint נכסי מתכון כך שיקבל גם קבצי וידאו:
  - `POST /api/recipes/{slug}/assets`
- הורחבה תמיכת וידאו לעשרות פורמטים, כולל `MP4`, `MOV`, `WebM`, `AVI`, `MKV`, `FLV`, `WMV`, `MPEG/MPG`, `3GP`, `TS/MTS/M2TS`, `VOB`, `MXF`, `RM/RMVB`, `DV`, `OGV` ועוד. MP4 בקידוד H.264 נשמר כפי שהוא; כל פורמט או קידוד אחר מומר אוטומטית עם FFmpeg ל-MP4/H.264/AAC עם `faststart` לניגון בדפדפן.
- נוספה בדיקת גישה לספק AI לפני שמירה, כדי לאמת API key בלי לבצע generation רגיל.
- נוספו endpoints למאמרים:
  - `GET /api/households/articles`
  - `POST /api/households/articles`
  - `GET /api/households/articles/{article_id}`
  - `PUT /api/households/articles/{article_id}`
  - `DELETE /api/households/articles/{article_id}`
  - `POST /api/households/articles/ai-create`
  - `POST /api/households/articles/browser-page`
  - `POST /api/households/articles/ai-search`
- יצירת מאמר עם AI באתר משתמשת עכשיו באותה זרימת `browser-page` של התוסף: אם ה-AI מזהה מתכון בתוך המאמר, אפשר לחלץ ממנו מתכון, ליצור עבורו רשימת קניות, לסדר אותה עם AI ולהביא תמונות לפריטים.
- נוספו endpoints לספריית הסרטונים ולהגדרות yt-dlp המשותפות לאתר ולתוסף:
  - `GET /api/households/videos`
  - `POST /api/households/videos`
  - `POST /api/households/videos/browser-page`
  - `GET /api/households/videos/settings`
  - `PUT /api/households/videos/settings`
  - `GET /api/households/videos/{video_id}`
  - `PUT /api/households/videos/{video_id}`
  - `POST /api/households/videos/{video_id}/retry`
  - `POST /api/households/videos/{video_id}/cancel`
  - `GET /api/households/videos/{video_id}/media`
  - `GET /api/households/videos/{video_id}/thumbnail`
  - `GET /api/households/videos/{video_id}/delete-preview`
  - `DELETE /api/households/videos/{video_id}`
- נוסף endpoint לסידור רשימת קניות עם AI:
  - `POST /api/households/shopping/lists/{item_id}/organize-ai`
- נוספו endpoints לאיחוד ולמחיקה מרוכזת של רשימות קניות:
  - `POST /api/households/shopping/lists/merge`
  - `DELETE /api/households/shopping/lists?ids={list_id}&ids={list_id}`
- נוסף endpoint לפתיחה/יצירה מהירה של רשימת קניות מתוך מתכון:
  - `POST /api/recipes/{slug}/shopping-list/open-or-create`
- נוסף endpoint להוספת תמונת מתכון אוטומטית עם AI/חיפוש תמונה ציבורית:
  - `POST /api/recipes/{slug}/image/ai`
- נוספו endpoints לשמירה וקריאה של מצב הקריאה בספר:
  - `GET /api/households/uploaded-books/reading-states`
  - `GET /api/households/uploaded-books/{book_id}/reading-state`
  - `PUT /api/households/uploaded-books/{book_id}/reading-state`
- נוסף endpoint להצעת ארוחה שלמה ממתכונים קיימים בעזרת AI:
  - `POST /api/households/mealplans/ai-suggest`
- נוספו endpoints לתמונות פריטים/כלים:
  - `POST /api/recipes/{slug}/item-images/ensure`
  - `POST /api/households/shopping/lists/{item_id}/item-images/ensure`
  - `GET /api/media/item-images/{group_id}/{kind}/{file_name}?name=...`

## הערות חשובות

- רשימות המודלים בספקי AI הן רשימות מובנות בקוד. מומלץ לעדכן אותן מדי פעם כי ספקי AI משנים מודלים, שמות ותמחור.
- בדיקת API key מנסה להימנע מקריאת generation בתשלום, אבל ספקים חיצוניים עדיין יכולים להטיל rate limits או לשנות מדיניות.
- FFmpeg ו-FFprobe נדרשים בסביבת השרת כדי להמיר פורמטים שאינם MP4/H.264. אם FFmpeg אינו מותקן או שהקובץ פגום, ההעלאה נכשלת עם הודעת המרה ברורה והקובץ החלקי נמחק.
- גודל המדיה העליונה כבר לא נשמר בדפדפן, כי הסליידר של המדיה העליונה הוסר והפריסה נקבעת אוטומטית.
- התמיכה הישירה ב-Anthropic מיועדת בעיקר לפיצ'רי טקסט. תמונה ואודיו נשארים תלויים בספקים שתומכים בפורמט המתאים.
- בחילוץ מתכונים או תרגום ספרים עם ספק Google Gemini, אם נשמרו כמה API keys באותו ספק, העבודה מתחלקת בין המפתחות לפי chunks. כל מפתח מתפקד כ-slot נפרד לאותה הרצה, ומפתח שנכשל בהרשאה מושבת רק לאותה עבודה.
- במחיקה מרוכזת של מתכונים שחולצו מספר אפשר למחוק מתכונים, את רשימות הקניות שנוצרו עבורם, או את שניהם. ברירת המחדל מסמנת את שניהם, והשיוך נעשה לפי מזהי הספר והמתכון שנשמרו ברשימה ולא לפי שם שעלול להיות זהה במקרה.
- רשימות קניות קיבלו פתיחת popup מהירה ליצירת רשימה חדשה, כולל אפשרות לחפש מתכון ולשייך אותו כבר בזמן יצירת הרשימה. אם לא מזינים שם, שם הרשימה מגיע מהמתכון שנבחר.
- איחוד רשימות קניות תומך בכל מספר רשימות שהמשתמש בוחר (מינימום שתיים), ולא מוגבל לשתי רשימות. חלונות האיחוד והמחיקה המרוכזת כוללים חיפוש מהיר ששומר את הבחירות גם כאשר הרשימה מסוננת, וכותרת חלון האיחוד משתמשת במפתח התרגום התקין.
- בניווט המהיר נוסף קישור "רשימה חדשה" מתחת לרשימות קניות, שפותח ישירות את popup היצירה.
- בתכנון ארוחות לחיצה על כרטיס של כל יום, גם יום שאינו היום הנוכחי, מעבירה בין מצב צפייה למצב עריכה תוך שמירת טווח התאריכים הנוכחי. היום הנוכחי ממשיך לקבל הדגשה חזותית בלבד.
- ביצירת תכנית ארוחות אפשר לחפש מתכונים, לסנן אותם לפי כמה קטגוריות ותגיות, ואז להמשיך לחפש בתוך התוצאות המסוננות. ניתן לבחור כמה מתכונים בפעולה אחת; כולם נשמרים לאותו יום ולסוג הארוחה שנבחר, ורענון הרשימה מתבצע פעם אחת בסיום.
- בספריית ספרי הבישול כרטיס ספר מקבל הדגשת hover/focus, ולחיצה ישירה על תמונת הכריכה פותחת את הספר. הכפתור הקיים לפתיחת הספר נשאר זמין.
- מצב "הגדרה חופשית" ביצירת ספר עם AI שולח את הטקסט החופשי כהגדרת הספר, כך שאפשר לכתוב למשל "צור ספר של ארוחות בוקר". הוא נשאר בלעדי למצב ההגדרות המוכנות, כפי שמוצג ב-toggle.

## יצירת ארוחה ותכנון ארוחות עם AI

- בכל יום במצב העריכה של תכנון הארוחות נוסף כפתור "תכנון ארוחה עם AI".
- סוג הארוחה הוא שדה חובה ומוגבל לארוחת בוקר, צהריים או ערב. המשתמש מתאר את הארוחה בטקסט חופשי, למשל "ארוחת ערב חלבית" או "ארוחת צהריים קיצית קלה".
- ניתן לבחור מתכון עוגן אופציונלי ולציין אם הוא מנה ראשונה, עיקרית או קינוח. ה-AI חייב להשאיר אותו בתפקיד שנבחר ולהתאים אליו את שאר הארוחה.
- לפני יצירת ההצעה ניתן לסנן את מאגר המועמדים לפי כמה קטגוריות, כמה תגיות, או שתיהן יחד. הבחירה היא `או` בין ערכים מאותו סוג ו־`וגם` בין קבוצת הקטגוריות לקבוצת התגיות; גם חיפוש מתכון העוגן מוגבל לאותו סינון.
- ה-AI מקבל אינדקס מסונן של המתכונים הקיימים בלבד, בוחר לכל היותר מנה ראשונה, עיקרית וקינוח, ומחזיר slugs קיימים בלבד. תשובה שממציאה מתכון או slug שאינו קיים נפסלת בצד השרת.
- ההצעה מוצגת באותו popup עם הסבר קצר ו-checkbox לכל מנה. המשתמש יכול להסיר מנה שאינו רוצה ורק אז להוסיף את הבחירה ליום שנבחר.
- מנות שנבחרו נשמרות כרשומות תכנון רגילות: המנה העיקרית מקבלת את סוג הבוקר/צהריים/ערב שנבחר, מנה ראשונה נשמרת כתוספת וקינוח נשמר כקינוח.
- כל הממשק החדש של יצירת הארוחה נוסף בעברית ובאנגלית; שפות אחרות ממשיכות להשתמש ב-fallback האנגלי בהתאם להחלטה הנוכחית של הפרויקט.
- החיפוש וההצעה אינם יוצרים cache בלתי מוגבל או polling. אפשרויות המתכונים מוגבלות לתוצאות החיפוש הקיימות, ולקוח ה-AI נסגר בסיום הקריאה דרך מנגנון השירות המשותף.

## ביקורת יציבות, RAM ו-CPU

- בוצעה סקירת `git diff` ממוקדת לנקודות שעלולות ליצור הצטברות משאבים:
  - timers
  - watchers
  - event listeners
  - object URLs
  - localStorage גדל
  - HTTP/API clients
  - משימות רקע והעלאות קבצים
- נוסף ניקוי מפורש ל-`AsyncOpenAI` אחרי קריאות chat completion ותמלול אודיו, כדי למנוע הצטברות connection pools או חיבורי HTTP פתוחים אחרי הרבה פעולות AI.
- בדיאלוג ספקי AI נוסף ניקוי ל-debounce timer של בדיקת API key כאשר הדיאלוג נסגר או הקומפוננטה יורדת מהמסך.
- תוצאות validation ישנות של API key מבוטלות לוגית אם הדיאלוג נסגר או אם המשתמש שינה את הנתונים בזמן שהבדיקה עדיין רצה.
- בניווט המהיר עודכן state של dropdowns כך שהוא נבנה מחדש לפי הסעיפים הנוכחיים, במקום לצבור מפתחות ישנים אם שמות/קבוצות משתנים.
- רענון סטטוס ספרים שהועלו קיבל guard נגד בקשות מקבילות, כדי ששרת איטי או בקשה תקועה לא יגרמו להצטברות polling requests כל 6 שניות.
- רענון סטטוס ספרים שהועלו עוצר את ה-polling אם אין יותר משתמש/קבוצה פעילה, כדי שלא יישאר timer שרץ ברקע אחרי מעבר הקשר.
- קאש ה-stores של ניווט ציבורי לפי קבוצות הוגבל ל-8 קבוצות אחרונות, כדי למנוע גידול לא מוגבל אם מדפדפים בין הרבה קבוצות ציבוריות.
- קאש ה-stores של ניווט ציבורי הועבר ל-`Map` לא ריאקטיבי. זה מתקן מסך לבן שנגרם מלולאת עדכון ב-`VApp` כאשר הניווט הציבורי עדכן cache מתוך computed getters.
- ברשימות קניות במצב פתיחה inline, נתוני רשימה מלאה נמחקים מה-state המקומי כאשר סוגרים את הרשימה או מכבים את מצב הפתיחה inline, כדי למנוע הצטברות זיכרון אחרי פתיחה של הרבה רשימות גדולות.
- בחירת רשימות לאיחוד/מחיקה, רשימות המקור הפתוחות ותוצאות החיפוש נשמרות רק ב-state של המסך ומתאפסות בסגירת החלון או ביציאה מהמסך; לא נוספו timers, listeners או polling עבור פיצ'ר האיחוד.
- חיפוש המתכונים בתכנון ארוחות מבטל לוגית תוצאות של בקשות ישנות, כך שהקלדה או שינוי מסננים מהיר לא מאפשרים לתגובה מאוחרת לדרוס את התוצאות העדכניות. בחירה מרובה יוצרת את הרשומות ברצף ומרעננת את התכנון פעם אחת, במקום להפעיל רענון נפרד לכל מתכון.
- יצירת מתכון ב-streaming URL מבטלת את משימת הרקע אם חיבור ה-SSE נסגר לפני שהעבודה הסתיימה, כדי שלא תמשיך משימת AI/ייבוא ארוכה בלי לקוח שמאזין לה.
- חיפוש AI ב"מצא מתכון" קיבל מזהה ריצה כדי שתוצאה ישנה לא תדרוס תוצאה חדשה, ו-loading משתחרר גם אם הבקשה מסתיימת בצורה חריגה.
- טופס "יצירה עם AI" קיבל טיפול חריגות כללי כדי שלא יישאר במצב טעינה אם העלאת וידאו או ייבוא URL נכשלים באופן לא צפוי.
- בנכסי מתכון נוסף ניקוי של הגדרות גודל וידאו מ-localStorage כאשר asset וידאו נמחק.
- וידאו בעמוד המתכון משתמש ב-`preload="metadata"` ולא טוען את כל הקובץ מראש.
- לא נמצאו `ObjectURL` חדשים ללא `revokeObjectURL` בשינויים שנוספו.
- העלאות וידאו מרובות לייבוא URL נשמרות זמנית בתיקיית temp ונמחקות ב-`finally` לאחר סיום משימת הייבוא.
- העלאת ספרי בישול נשמרת בצ'אנקים של 1MB לתיקיית `DATA_DIR/uploaded-books`, כדי לא לקרוא ספרים גדולים לזיכרון בבת אחת.
- לאחר חלוקת ספר ל-chunks עבור תרגום או חילוץ מתכונים, רשימת העמודים המקורית משוחררת מהזיכרון כדי לא להחזיק טקסט ספרים גדול פעמיים לאורך ריצת ה-AI.
- קבצים שעלולים להריץ תוכן בדפדפן, כמו HTML, מוגשים כהורדה ולא כ-inline preview.
- בתרגום ספרים ובחילוץ מתכונים מספרים, מפתח AI שמחזיר שגיאת הרשאה/מפתח לא תקין מושבת רק לאותה הרצה, והחלק שנכשל נשלח מחדש למפתח AI אחר אם קיים. רק אם לא נשאר אף מפתח שמיש החלק מסומן כנכשל.
- עבודות AI ארוכות על ספרים קיבלו מנגנון המשך אוטומטי דרך ה-scheduler: אם תרגום/חילוץ ספר נמצא ב-`processing` או `retrying` אחרי restart או אחרי המתנה ל-rate limit, המערכת תמשיך את החלקים שלא הסתיימו ותדלג על chunks שכבר הושלמו.
- לכל chunk שמחכה ל-rate limit נשמר `nextRetryAt` אמיתי, כך שהמערכת יודעת מתי לנסות שוב גם אם הדוקר או המחשב הופעלו מחדש.
- אם ספר הסתיים ב-`partial_failed` ולוחצים שוב על אותה פעולה עם אותם פרמטרים, המערכת ממשיכה את החלקים שנכשלו במקום למחוק עבודה שכבר הצליחה.
- שפת חילוץ מתכונים מספרים נשמרת במסד הנתונים, כדי שחידוש אוטומטי אחרי restart ימשיך באותה שפה שנבחרה.
- חלון "משימות ברקע" משתמש באותו polling קיים של ספרים, שמופעל רק כשיש תהליכי ספרים פעילים, ולכן לא נוסף timer קבוע חדש.
- ביטול משימות ספרים לא הורג thread בכוח אלא מסמן סטטוס במסד וגורם ל-worker לצאת בין chunks; זה מונע מצב של כתיבה חלקית לא עקבית או connection שנחתך באמצע קריאת AI.
- תמונות רכיבים/כלים נשמרות בקאש לפי hash של שם מנורמל, ולכן אותו פריט לא מוריד שוב תמונה בכל מתכון או רשימת קניות.
- הורדת תמונות פריטים מוגבלת ל-4 הורדות במקביל, מעובדת באצוות של עד 40 פריטים, מוגבלת ל-8MB לקובץ, וקובץ זמני נמחק ב-`finally` גם אם ההורדה או המזעור נכשלים.
- חיפוש והורדת תמונות באותה פעולה משתמשים בשני HTTP clients משותפים עם connection pools מוגבלים, במקום לפתוח client וחיבור חדש עבור כל פריט. שני ה-clients נסגרים אוטומטית בסיום הפעולה.
- עיבוד Pillow של תמונות פריטים הועבר מ-event loop ל-thread של Python, כדי שעיבוד WebP לא יחסום בקשות אחרות. לפני הפענוח המלא נאכפת גם מגבלה של 25 מיליון פיקסלים, בנוסף למגבלת 8MB, כדי למנוע קפיצת RAM מתמונה דחוסה בעלת ממדים חריגים.
- חיבור האתר לתוסף הדפדפן מנקה מיד את listener ההודעות ושני ה-timeouts אם עוזבים את חלון "יצירה עם AI" באמצע, במקום להשאיר אותם עד timeout של שלוש דקות.
- תוקנה קריסת Vue שהשאירה את תוכן הדף לבן במעבר לרשימות קניות ולמסכים שמשתמשים בתמונות פריטים. הסיבה הייתה שימוש ב-`useStaticRoutes` ללא import מפורש בארבעה רכיבים; כל מסלולי הניווט המרכזיים נבדקו לאחר התיקון.
- תוקנה שגיאת SSR מסוג `500` שנגרמה משימוש ב-`useUserApi` ללא import מפורש בכרטיס המידע של דף המתכון, שבו נבנה קישור לעמוד המקור בספר. לאחר התיקון עמוד הבית ודף מתכון נבדקו בדפדפן ללא שגיאת `useUserApi is not defined`.
- תצוגת תמונות פריטים משתמשת ב-`loading="lazy"` ו-`decoding="async"`, כך שתמונות לא נטענות בבת אחת ולא חוסמות את הצגת הדף.
- רענון מאוחר של סיווג ספר שומר את מזהה ה-`setTimeout`, מבטל timer קודם ומנקה אותו ב-`onBeforeUnmount`, כך שמעבר חוזר בין מסכי ספרים לא משאיר קריאות רענון ישנות.
- סיווג ספר גדול אינו מחלץ עוד את כל הספר לזיכרון: ב-PDF וב-EPUB הוא קורא לכל היותר 40 עמודי דגימה, ומתוכם שולח עד 12 עמודים רלוונטיים ל-AI. תרגום וחילוץ מתכונים מלאים ממשיכים לעבוד על הטווח שהמשתמש ביקש.
- תכנון תוכן העניינים של ספר AI עובד במנות של עד 80 מתכונים. כך גם אוסף של מאות או אלפי מתכונים אינו נשלח כ-JSON ענק בקריאה אחת.
- העתקת קובץ ספר שהועלה הועברה ל-thread ייעודי ונשארה בצ'אנקים של 1MB, כך שהעלאת ספר גדול אינה חוסמת את event loop של ה-API.
- FFmpeg ו-FFprobe רצים עם timeout, נאספים באמצעות `communicate`, נהרגים במקרה חריגה, וקובץ המרה חלקי נמחק בכל כשל. המרה מפורמט אחר גם מסרבת לדרוס MP4 קיים בעל אותו שם.
- ריבוי מפתחות Gemini אינו מוגבל במספר workers קבוע: 20 מפתחות ייחודיים יוצרים 20 slots. מספר ה-workers בפועל הוא המינימום בין מספר המפתחות למספר ה-chunks, ולכן לא נוצרים workers ריקים.
- אחוזי התרגום ברשימת השפות מחושבים מקובצי התרגום המקומיים ומאימות placeholders, במקום להציג נתוני Crowdin ישנים. עברית ואנגלית כוללות גם את חמש המחרוזות האחרונות; יתר השפות מציגות את האחוז האמיתי ומשתמשות ב-fallback אנגלי עבורן.
- מצב השפה האוטומטי בתוסף קורא קודם את שפת אתר Mealie, ונופל לשפת הדפדפן רק אם אין שפת אתר זמינה. בחירה ידנית נשמרת לתוסף בלבד ואינה משנה את שפת האתר.
- איחוד רשימות קניות אינו מבצע עוד השוואה של כל פריט מול כל שאר הפריטים. פריטים חדשים וקיימים מאונדקסים לפי רשימת יעד וזהות מוצר/טקסט, כך שעומס ה-CPU באיחוד רשימות גדולות נשאר קרוב ללינארי במקום לגדול ריבועית.
- בעת איחוד רשימות, שלב האימות הראשוני טוען רק תקצירי רשימות ללא כל הפריטים. לאחר יצירת הרשימה המאוחדת פריטי המקור מוזרמים רשימה אחר רשימה, במקום להחזיק בזיכרון את כל רשימות המקור וגם עותק מלא נוסף בו-זמנית.
- אינדקס האיחוד כולל את מזהה רשימת היעד ומונע איחוד מקרי של מוצרים זהים שנשלחו באותה פעולת bulk לשתי רשימות שונות.
- תכנון ארוחות אינו משתמש עוד ב-`cache-items` הבלתי מוגבל של Vuetify. נשמרות רק מאה תוצאות החיפוש הנוכחיות והמתכונים שנבחרו בפועל, והקאש הקטן מתאפס בסגירת החלון.
- סינון יצירת ארוחה עם AI סורק פעם אחת את אינדקס המתכונים הקיים, משתמש ב-`set` מקומי להשוואת קטגוריות ותגיות וממשיך להגביל את מספר המועמדים שנשלחים לספק. לא נוספו polling, timers, listeners או cache חדש עבור הסינון או התאמת הארוחות.
- רענוני רשימות קניות, קטגוריות ותגיות בניווט המהיר עוברים coalescing: בכל רגע רצה לכל היותר בקשה אחת מכל סוג, ואירועים נוספים בזמן בקשה קיימת גורמים לכל היותר לרענון המשך יחיד.
- מסך רשימות הקניות בונה אינדקס קבוע לפי מזהה רשימה במקום לסרוק שוב את כל הרשימות עבור כל רשימה מאוחדת. ביציאה מהמסך מתאפסים גם תוצאות חיפוש, רשימות inline, טיוטות עריכה ו-sets זמניים.
- חיפוש מתכונים משחרר תמיד את מצב הטעינה ב-`finally`, גם אם לקוח ה-API זורק חריגה, וממשיך להתעלם מתגובה ישנה אם בקשה חדשה כבר התחילה.
- חיפוש באתרי קניות מסונן בשאילתת SQL לפי הקבוצה במקום לטעון את כל האתרים לזיכרון ולסנן ב-Python. תווים מיוחדים בחיפוש עוברים escaping לפני בניית תבנית החיפוש.
- קריאת דף אתר לצורך ניתוח AI מוגבלת ל-2MB של תגובת רשת ול-250,000 תווי טקסט נקי. ה-HTTP client נסגר אוטומטית בסיום, גם במקרה של timeout או שגיאת אתר.
- חיפוש, קיפול וגלילה בכל מקטעי הניווט המהיר מבוססים על computed/local reactive state ועל ערך localStorage מוגבל של חמישה flags בלבד; לא נוספו timers, polling או listeners גלובליים.
- שינוי פריסת popup התוסף אינו מוסיף listeners או timers עבור כפתור "שמור אתר". מנגנון בדיקת החיבור משתמש ב-timer יחיד שעובר debounce, מאפס את ה-reference לאחר ההפעלה ומבטל באמצעות `AbortController` בדיקת רשת קודמת לפני התחלת בדיקה חדשה.
- מיון רשימות קניות מתבצע על מערך תוצאות מסונן חדש ואינו משנה את cache ה-API. ספירת הפריטים מבוצעת בשאילתת SQL אחת עם אינדקס קיים על `shopping_list_id`, בלי N+1 של טעינת פריטי רשימה.
- פעולות השיוך החדשות למתכון, רשימת קניות, ספר AI ואתר הן בקשות נקודתיות בלבד. הן אינן מוסיפות polling, timers, listeners גלובליים או caches לא מוגבלים; חלונות ה-popup משחררים את מערכי החיפוש והבחירה עם סגירתם.
- ספר מתורגם מוסיף `IntersectionObserver` יחיד לעדכון העמוד הפעיל וההתקדמות, ומנתק אותו באירוע `pagehide`. תוכן העניינים והמתכונים המשובצים הם HTML סטטי ואינם מוסיפים polling, timers או cache שגדל בזמן הקריאה.
- איתור מתכונים שחולצו מספר מבוצע בשאילתת SQL לפי `uploadedBookSourceId` או fallback של מקור, במקום לטעון את כל מתכוני הקבוצה לזיכרון. בחירת רשימות/מתכונים למחיקה נשארת מוגבלת לרשומות המקושרות לספר ולמשק הבית הנוכחי.
- סימון קיום רשימת קניות בכרטיסי מתכון משתמש במספר קבוע של שאילתות bulk עבור העמוד הנוכחי בלבד, במקום N+1 בקשות או cache שגדל לאורך זמן. חלון הרשימה המהיר מבטל לוגית תגובות ישנות כשהוא נסגר, משחרר את הרשימה והטיוטות המקומיות ואינו מוסיף timer או listener גלובלי.
- fallback הקישור של תוסף הדפדפן ממחזר טאב מקור קיים. טאב זמני נוצר רק כשאין טאב מתאים, listener הטעינה וה-timeout מתנקים בשני מסלולי הסיום, ורק הטאב הזמני נסגר ב-`finally`.
- במסלול תמונת הכיסוי אין retry loop או cache חדש: ניסיון התמונה האוטומטית נשאר מוגבל במספר מועמדים, ובדיקת fallback ידני מוסיפה לכל היותר קריאת מתכון נקודתית אחת.
- לאחר ביקורת שלמות התרגום משתחררים במפורש מערכי ה-chunks, מפת העמודים הכפולה ומחרוזת ה-HTML הגדולה לאחר הכתיבה, כך שטקסט הספר אינו נשאר בכמה עותקים לאורך שלב הסיווג האחרון.
- עבודות עיבוד וידאו כבדות מוגבלות לשתי עבודות פעילות במקביל. יתר העבודות נשארות ממתינות ב-event loop בלי להפעיל FFmpeg/yt-dlp נוספים, וסט המשימות הפעילות מנקה כל משימה עם סיומה.
- חיבור SSE של יצירת מתכון נסגר בכל מסלול סיום, שגיאת שרת, הודעה לא תקינה או ניתוק, וה-Promise נפתר פעם אחת בלבד. כך listener וחיבור רשת אינם נשארים פתוחים אחרי כשל.
- טיימרי polling ורענון מאוחר במסכי סרטונים, ספרים ובניווט הראשי בודקים שהקומפוננטה עדיין mounted לפני שהם קובעים timer נוסף. יציאה מהמסך מבטלת את ה-timer ומונעת ממנו להיווצר מחדש אחרי תשובת API מאוחרת.
- לולאות יצירת שמות ייחודיים למתכונים ולמאמרים הוגבלו ל-10,000 ניסיונות ומקבלות fallback מבוסס UUID, כך שגם מסד חריג עם אלפי כפילויות אינו יכול להחזיק request בלולאה בלתי מוגבלת.
- תרגום חלקי אינו מחזיק מחדש את הספר המלא כדי לשמור הצלחות: ביקורת השלמות עובדת על מפת העמודים שכבר נבנתה, מתעדת חסרים ומשחררת את מבני הביניים לאחר כתיבת התוצר.
- עבור השינויים האחרונים בוצעה סקירת משאבים סטטית ברמת הקוד בלבד, לפי בקשת המשתמש. לא הורצו דגימות RAM/CPU או בדיקות עומס; כן הורצו בדיקות תחביר, lint, build ובדיקות יחידה ממוקדות שאינן בדיקות משאבים.

## בדיקות שבוצעו

במהלך העבודה הורצו בדיקות frontend נקודתיות וגם build מלא:

- `npm exec eslint -- ...`
- `npm run build`
- שש בדיקות יחידה ממוקדות עברו עבור ביקורת שלמות התרגום, יצירת ספר HTML, שמירת תוכן עניינים מקורי ומספרי עמודים מודפסים, בניית היררכיית פרק/מתכון ממטא־דאטה של AI, והחרגת מתכונים שמחוץ לטווח תרגום חלקי.
- 16 בדיקות שירות ממוקדות נוספות עברו בתוך תמונת Docker/Linux עבור חילוץ/תרגום/סיווג ספרים, יצירת ספר AI ושפת הסידור של רשימות קניות.
- `python -m py_compile ...` לקבצי backend שנגעו ב-AI/ייבוא/מתכונים
- 70 בדיקות integration ויחידה ממוקדות עברו בתוך קונטיינר ה-API. הן מכסות איחוד רשימות קניות, מסלולי SPA, שינוי כמויות מתכון עם AI, שפת רשימת קניות, חילוץ/תרגום ספרים ואוספי מתכונים, תמונות רכיבים וכלים, מגבלות תמונה וקבצים זמניים, המרת וידאו וניקוי כשל, מניעת דריסת MP4, בחירת כל המתכונים במנות AI, validation להגדרת ספר, פיצול לכרכים, תכנון במנות, דגימת PDF מוגבלת, חפיפת עמודי הקשר בחילוץ, יצירת 20 workers מ-20 מפתחות Gemini, ספירת פריטים בתקציר רשימת קניות ו-CRUD/חיפוש לאתרי קניות.
- `python -m ruff check ...` עבר על כל קובצי ה-backend והבדיקות שנגעו בשינוי.
- `git diff --check`
- בדיקות JSON לקבצי תרגום
- בבדיקה האחרונה עברו `ruff` ו-`py_compile` על 45 קובצי Python ששונו או נוספו, ESLint עבר על כל קובצי Vue/TypeScript/JavaScript ששונו, ושבעת קובצי JSON הפעילים של עברית/אנגלית והתוסף נטענו בהצלחה.
- `python dev/scripts/check_localization_parity.py ...` עבר בעבר על בסיס 42 השפות, 1,700 מפתחות האתר ו-80 מפתחות התוסף. הפיצ'רים החדשים נשארו כרגע בעברית ובאנגלית בלבד בהחלטה מפורשת ומשתמשים ב-fallback אנגלי בשפות האחרות.
- בדיקות DOM לתוסף עברו בעברית ובערבית (RTL) ובגרמנית ובסינית מסורתית (LTR): ה-locale וכיוון התצוגה הוחלו, הופיעו 42 יעדי תרגום ו-42 שפות ממשק לצד מצב אוטומטי. נבדק גם שמצב אוטומטי מעדיף את cookie השפה של Mealie ושבחירה ידנית משנה רק את ה-popup.
- נבדקו בנפרד גם שיתוף ה-i18n בין content scripts והטעינה ב-background service worker. ארבעת קובצי ה-JavaScript של התוסף עברו `node --check`, וגרסת manifest `0.2.2` נטענה ללא שגיאת JSON או locale. פריסת popup נמדדה ברוחב 420px ובגובה 552px, וכפתור "שמור אתר" הסתיים ב-540px ונשאר גלוי ללא גלילה במצב הרגיל.
- כל חמשת מקטעי הניווט המהיר נבדקו בדפדפן. קיפול ופתיחה נשמרו לאחר רענון, חיפוש ברשימות קניות סינן 83 רשימות, וכל מקטע ארוך הוגבל בפועל לגובה 260px עם `overflow-y: auto`.
- לאחר השינויים בוצעו שש דגימות רצופות לקונטיינר המאוחד: השימוש נשאר יציב בטווח 355.5-355.7MiB, מספר התהליכים נשאר 21 וה-CPU נשאר בטווח 0.12%-0.15%. לא נמצאו שורות ERROR/CRITICAL, traceback, OOM או שגיאות לא מטופלות בלוגים העדכניים, ו-`/api/app/about` החזיר `200`.
- בוצעה בדיקת כתב אוטומטית לערבית, יוונית, עברית, יפנית, קוריאנית, רוסית, אוקראינית וסינית כדי לזהות בטעות קובץ שנשאר ברובו באנגלית.
- בדיקות זמינות מקומית ל-frontend/backend (`200` ב-localhost)
- נבנתה והופעלה תמונת `mealie-custom:dev` המאוחדת, והקונטיינר עבר healthcheck. נבדקו `200` עבור `/`, ‏`/g/home` ו-`/api/app/about`, לצד `404` תקין עבור API ו-asset שאינם קיימים.
- הבנייה הנוכחית של `docker-compose.custom.yml` הושלמה עם frontend ו-backend באותה תמונת production. הקונטיינר קיבל `healthy`, ו-`/g/home` ו-`/api/app/about` החזירו `200`. לא הורצו בבדיקה זו דגימות RAM/CPU או בדיקות עומס.
- בתוך התמונה הבנויה אומת שחוזה שפת היעד עבור `he-IL` מחייב עברית גם בכל שדות המתכון וגם בקטגוריות ובהערות של רשימת הקניות.
- בדיקת integration לאיחוד רשימות אימתה בחירה של יותר משתי רשימות ואיחוד כמויות מובנות מ-`300`, ‏`500` ו-`200` ל-`1,000`, שמירת כל שלוש רשימות המקור, מחיקה ממוקדת ושמירת פריטי טקסט לא-מובנים נפרדים.

בדיקת קצה אמיתית בוצעה על `White Heat` של Marco Pierre White:

- קובץ המקור כולל 212 עמודים, מהם 199 עמודים עם שכבת OCR שניתנת לחילוץ.
- תרגום מלא לעברית הסתיים ב-20 מתוך 20 chunks ונבנה לספר HTML אחד. התוצר כולל את כל 199 עמודי ה-OCR בלי עמוד חסר וכ-96,000 תווים בעברית; עמודים שבהם ה-OCR הכיל רק רעש תמונה נשמרו כעמודים ריקים.
- חילוץ מלא הסתיים ב-20 מתוך 20 chunks. לאחר השוואה מול אינדקס המתכונים המודפס בסוף הספר נשמרו 73 מתוך 73 ההכנות והמתכונים בעברית, כולל בסיסים קצרים כמו חמאה מזוקקת, ז'ו דה נאז', טליאטלה ורביולי. לכל אחד מהם יש רכיבים, הוראות, קטגוריות, תגיות ומקור עם עמוד/טווח עמודים.
- כל 14 מפתחות Gemini הייחודיים נוסו. 10 מפתחות שמישים חילקו ביניהם את העבודה; ארבעה מפתחות שהחזירו `403 PERMISSION_DENIED` הושבתו רק לאותה ריצה וה-chunks שלהם הוחזרו לתור ועברו למפתחות תקינים.
- שגיאות `429` זמניות טופלו ב-cooldown וב-retry על מפתח אחר, בלי להשאיר chunk חסר.
- בדיקת חפיפה נוספת מצאה מתכון שחצה גבול של 10 עמודים והשלימה עוד הכנות שהמודל דילג עליהן בריצה הראשונה. הכפילויות שנוצרו רק מהרצות האימות החוזרות נוקו דרך שירות המחיקה של Mealie, והספירה הסופית אומתה מול 73 הרשומות באינדקס הספר.

ה-build עובר, אך קיימות אזהרות קיימות של הפרויקט לגבי chunk size / circular re-export / Vuetify CSS minify. האזהרות אינן קשורות ישירות לשינויים של ה-fork ולא עצרו את ה-build.
